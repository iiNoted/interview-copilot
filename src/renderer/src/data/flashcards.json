{
  "version": 1,
  "categories": [
    {
      "id": "dsa",
      "name": "Data Structures & Algorithms",
      "icon": "Binary",
      "description": "Arrays, trees, graphs, sorting, dynamic programming",
      "color": "emerald"
    },
    {
      "id": "system-design",
      "name": "System Design",
      "icon": "Network",
      "description": "Scalability, distributed systems, architecture",
      "color": "blue"
    },
    {
      "id": "web-frontend",
      "name": "Web / Frontend",
      "icon": "Globe",
      "description": "HTML, CSS, React, DOM, browser APIs",
      "color": "orange"
    },
    {
      "id": "backend-apis",
      "name": "Backend & APIs",
      "icon": "Server",
      "description": "REST, GraphQL, authentication, middleware",
      "color": "violet"
    },
    {
      "id": "databases-sql",
      "name": "Databases & SQL",
      "icon": "Database",
      "description": "SQL queries, indexing, normalization, NoSQL",
      "color": "cyan"
    },
    {
      "id": "devops-cloud",
      "name": "DevOps & Cloud",
      "icon": "Cloud",
      "description": "CI/CD, containers, AWS/GCP/Azure, monitoring",
      "color": "amber"
    },
    {
      "id": "oop-design-patterns",
      "name": "OOP & Design Patterns",
      "icon": "Blocks",
      "description": "SOLID, creational/structural/behavioral patterns",
      "color": "pink"
    },
    {
      "id": "javascript",
      "name": "JavaScript",
      "icon": "FileCode2",
      "description": "Closures, promises, event loop, ES6+",
      "color": "yellow"
    },
    {
      "id": "python",
      "name": "Python",
      "icon": "FileCode2",
      "description": "Generators, decorators, GIL, data model",
      "color": "sky"
    },
    {
      "id": "java",
      "name": "Java",
      "icon": "FileCode2",
      "description": "JVM, generics, concurrency, Spring",
      "color": "red"
    },
    {
      "id": "cybersecurity",
      "name": "Cybersecurity",
      "icon": "Shield",
      "description": "Network security, encryption, OWASP, incident response, compliance",
      "color": "red"
    }
  ],
  "cards": [
    {
      "id": "dsa-001",
      "categoryId": "dsa",
      "question": "What is the time complexity of accessing an element by index in an array vs a linked list, and why?",
      "answer": "Array access by index is O(1) because elements are stored in contiguous memory, so the address is computed directly via base + offset. Linked list access is O(n) because you must traverse from the head node, following pointers one by one until you reach the desired position.",
      "difficulty": "easy",
      "tags": [
        "arrays",
        "linked-lists",
        "time-complexity"
      ]
    },
    {
      "id": "dsa-002",
      "categoryId": "dsa",
      "question": "When would you choose a linked list over an array?",
      "answer": "Choose a linked list when you need frequent insertions and deletions at arbitrary positions, since these are O(1) once you have a reference to the node, versus O(n) for arrays due to shifting. Linked lists also avoid the cost of resizing that dynamic arrays incur. However, if you need random access or cache-friendly iteration, arrays are superior.",
      "difficulty": "easy",
      "tags": [
        "linked-lists",
        "arrays",
        "trade-offs"
      ]
    },
    {
      "id": "dsa-003",
      "categoryId": "dsa",
      "question": "Explain how a hash map handles collisions and what the worst-case lookup time is.",
      "answer": "The two main collision resolution strategies are chaining (each bucket stores a linked list of entries) and open addressing (probing for the next empty slot). With chaining, worst-case lookup degrades to O(n) if all keys hash to the same bucket. Java 8+ mitigates this by converting chains longer than 8 into balanced trees, giving O(log n) worst case per bucket. A good hash function and load factor below 0.75 keep average lookup at O(1).",
      "difficulty": "medium",
      "tags": [
        "hash-maps",
        "collision-resolution",
        "time-complexity"
      ]
    },
    {
      "id": "dsa-004",
      "categoryId": "dsa",
      "question": "What is the difference between a stack and a queue? Give a real-world use case for each.",
      "answer": "A stack is LIFO (last-in, first-out) — think of the browser back button, where the most recently visited page is popped first. A queue is FIFO (first-in, first-out) — think of a print job queue where documents are printed in submission order. Both support O(1) push/enqueue and pop/dequeue operations.",
      "difficulty": "easy",
      "tags": [
        "stacks",
        "queues",
        "data-structures"
      ]
    },
    {
      "id": "dsa-005",
      "categoryId": "dsa",
      "question": "How does a binary search tree differ from a balanced BST like an AVL tree, and why does it matter?",
      "answer": "A plain BST can degrade to a linked list if elements are inserted in sorted order, giving O(n) operations. An AVL tree maintains a balance factor (height difference between left and right subtrees is at most 1) and performs rotations on insert/delete to stay balanced. This guarantees O(log n) search, insert, and delete. The trade-off is that AVL rotations add overhead to writes, making it better suited for read-heavy workloads.",
      "difficulty": "medium",
      "tags": [
        "trees",
        "bst",
        "avl-tree",
        "balancing"
      ]
    },
    {
      "id": "dsa-006",
      "categoryId": "dsa",
      "question": "Explain the difference between BFS and DFS on a graph. When would you prefer one over the other?",
      "answer": "BFS explores level by level using a queue and finds the shortest path in unweighted graphs, so use it for problems like shortest distance or level-order traversal. DFS explores as deep as possible using a stack (or recursion) and is better for problems like cycle detection, topological sort, or exhaustive path exploration. BFS uses O(V) space for the queue in the worst case; DFS uses O(V) for the recursion stack but is generally more memory-efficient on wide graphs.",
      "difficulty": "medium",
      "tags": [
        "graphs",
        "bfs",
        "dfs",
        "traversal"
      ]
    },
    {
      "id": "dsa-007",
      "categoryId": "dsa",
      "question": "Walk me through how Dijkstra's algorithm works and its time complexity.",
      "answer": "Dijkstra's finds the shortest path from a source to all vertices in a weighted graph with non-negative edges. It maintains a min-heap of (distance, vertex) pairs, greedily extracting the closest unvisited vertex and relaxing its neighbors. With a binary heap, the complexity is O((V + E) log V). It fails on negative edge weights because it assumes once a node is finalized, no shorter path exists — for negative weights, use Bellman-Ford at O(VE).",
      "difficulty": "hard",
      "tags": [
        "graphs",
        "dijkstra",
        "shortest-path",
        "greedy"
      ]
    },
    {
      "id": "dsa-008",
      "categoryId": "dsa",
      "question": "What is the time complexity of mergesort and why is it preferred for sorting linked lists?",
      "answer": "Mergesort runs in O(n log n) in all cases — best, average, and worst — with O(n) auxiliary space for arrays. For linked lists, the merge step can be done in-place by relinking pointers, reducing extra space to O(log n) for recursion. Unlike quicksort, mergesort doesn't need random access, which makes it ideal for linked lists where index-based access is O(n).",
      "difficulty": "medium",
      "tags": [
        "sorting",
        "mergesort",
        "linked-lists",
        "time-complexity"
      ]
    },
    {
      "id": "dsa-009",
      "categoryId": "dsa",
      "question": "Compare quicksort and mergesort. When would you pick one over the other?",
      "answer": "Quicksort averages O(n log n) with O(log n) space (in-place), but has O(n²) worst case on already-sorted input without randomized pivot selection. Mergesort guarantees O(n log n) but needs O(n) extra space. In practice, quicksort is faster due to better cache locality and smaller constant factors — most standard library sorts (like C's qsort or Java's Arrays.sort for primitives) use a quicksort variant. Choose mergesort when you need stability or are sorting linked lists.",
      "difficulty": "medium",
      "tags": [
        "sorting",
        "quicksort",
        "mergesort",
        "trade-offs"
      ]
    },
    {
      "id": "dsa-010",
      "categoryId": "dsa",
      "question": "Explain dynamic programming. How do you decide if a problem can be solved with DP?",
      "answer": "Dynamic programming applies when a problem has overlapping subproblems (same subproblem solved multiple times) and optimal substructure (optimal solution builds from optimal solutions of subproblems). The two signals are: a recursive solution with repeated work, and the ability to define a recurrence relation. You can implement it top-down with memoization or bottom-up with tabulation. Classic examples include Fibonacci, knapsack, and longest common subsequence.",
      "difficulty": "medium",
      "tags": [
        "dynamic-programming",
        "recursion",
        "optimization"
      ]
    },
    {
      "id": "dsa-011",
      "categoryId": "dsa",
      "question": "Solve the 0/1 knapsack problem. What is the time and space complexity?",
      "answer": "Define dp[i][w] as the maximum value using the first i items with capacity w. The recurrence is dp[i][w] = max(dp[i-1][w], dp[i-1][w-weight[i]] + value[i]) if weight[i] <= w. Time is O(nW) where n is number of items and W is capacity. Space can be optimized from O(nW) to O(W) by using a single row and iterating capacity in reverse to avoid using updated values from the current row.",
      "difficulty": "hard",
      "tags": [
        "dynamic-programming",
        "knapsack",
        "optimization"
      ]
    },
    {
      "id": "dsa-012",
      "categoryId": "dsa",
      "question": "What is a greedy algorithm, and how does it differ from dynamic programming?",
      "answer": "A greedy algorithm makes the locally optimal choice at each step, hoping it leads to a global optimum. Unlike DP, it never reconsiders past choices. Greedy works when the problem has the greedy-choice property (a local optimum leads to a global one) and optimal substructure. Classic examples: activity selection, Huffman coding, and Kruskal's MST. If a greedy approach fails (like for 0/1 knapsack), you fall back to DP.",
      "difficulty": "easy",
      "tags": [
        "greedy",
        "dynamic-programming",
        "algorithm-design"
      ]
    },
    {
      "id": "dsa-013",
      "categoryId": "dsa",
      "question": "Implement binary search and explain when it can be applied beyond sorted arrays.",
      "answer": "Binary search repeatedly halves the search space by comparing the target with the middle element, running in O(log n). Beyond sorted arrays, it applies to any monotonic function — for example, finding the minimum capacity to ship packages within D days, or the square root of a number. The key insight is: if you can frame the problem as 'find the boundary where a condition flips from false to true,' binary search works.",
      "difficulty": "medium",
      "tags": [
        "binary-search",
        "arrays",
        "optimization"
      ]
    },
    {
      "id": "dsa-014",
      "categoryId": "dsa",
      "question": "What is the sliding window technique and when do you use it?",
      "answer": "Sliding window maintains a window (subarray/substring) that expands or contracts as you iterate, avoiding the O(n²) brute force of checking all subarrays. Use it when the problem asks for the longest/shortest subarray or substring satisfying some condition — like maximum sum subarray of size k, or longest substring without repeating characters. The window typically moves with two pointers, giving O(n) time.",
      "difficulty": "easy",
      "tags": [
        "sliding-window",
        "two-pointers",
        "arrays"
      ]
    },
    {
      "id": "dsa-015",
      "categoryId": "dsa",
      "question": "Explain the two-pointer technique. Give two distinct problems it solves.",
      "answer": "Two pointers uses two indices moving through a data structure, typically from opposite ends or at different speeds. Example 1: finding a pair that sums to a target in a sorted array — start pointers at both ends, move left pointer right if sum is too small, right pointer left if too large, O(n). Example 2: detecting a cycle in a linked list with Floyd's slow/fast pointers — if they meet, there's a cycle, O(n) time O(1) space.",
      "difficulty": "easy",
      "tags": [
        "two-pointers",
        "arrays",
        "linked-lists"
      ]
    },
    {
      "id": "dsa-016",
      "categoryId": "dsa",
      "question": "What is a trie and when would you use one instead of a hash map?",
      "answer": "A trie (prefix tree) stores strings character by character, where each node represents a prefix. It supports insert, search, and prefix lookup all in O(m) time where m is the word length. Use a trie over a hash map when you need prefix-based operations like autocomplete, spell checking, or counting words with a given prefix. A hash map gives O(1) exact lookup but can't efficiently enumerate all keys sharing a prefix.",
      "difficulty": "medium",
      "tags": [
        "tries",
        "data-structures",
        "strings"
      ]
    },
    {
      "id": "dsa-017",
      "categoryId": "dsa",
      "question": "How would you detect a cycle in a directed graph?",
      "answer": "Use DFS with three states per node: unvisited, in-progress (on the current recursion stack), and completed. If you encounter an in-progress node during DFS, you've found a back edge, which means a cycle exists. This runs in O(V + E). Alternatively, attempt a topological sort using Kahn's algorithm (BFS with in-degree tracking) — if the result contains fewer than V nodes, the graph has a cycle.",
      "difficulty": "medium",
      "tags": [
        "graphs",
        "cycle-detection",
        "dfs",
        "topological-sort"
      ]
    },
    {
      "id": "dsa-018",
      "categoryId": "dsa",
      "question": "What is a min-heap and how is it typically implemented? What operations does it support and at what cost?",
      "answer": "A min-heap is a complete binary tree where every parent is smaller than its children, so the minimum is always at the root. It's implemented as an array where for index i, children are at 2i+1 and 2i+2. Insert (bubble up) and extract-min (swap root with last, sift down) are both O(log n). Peek-min is O(1). Building a heap from n elements is O(n) using bottom-up heapify, not O(n log n).",
      "difficulty": "easy",
      "tags": [
        "heaps",
        "data-structures",
        "time-complexity"
      ]
    },
    {
      "id": "dsa-019",
      "categoryId": "dsa",
      "question": "Explain the difference between a red-black tree and an AVL tree. Which would you choose for a database index?",
      "answer": "Both are self-balancing BSTs with O(log n) operations. AVL trees are more strictly balanced (height difference ≤ 1 per node), giving faster lookups but slower inserts/deletes due to more rotations. Red-black trees allow slightly more imbalance (path lengths can differ by up to 2x) but need fewer rotations on modifications. For a database index with mixed reads and writes, red-black trees are typically preferred — most implementations (like Java's TreeMap and Linux's CFS scheduler) use them for this reason.",
      "difficulty": "hard",
      "tags": [
        "trees",
        "red-black-tree",
        "avl-tree",
        "databases"
      ]
    },
    {
      "id": "dsa-020",
      "categoryId": "dsa",
      "question": "How does counting sort work, and when is it better than comparison-based sorts?",
      "answer": "Counting sort counts occurrences of each value in a range [0, k], then computes prefix sums to determine output positions. It runs in O(n + k) time and space. It beats O(n log n) comparison sorts when k is roughly O(n) — for example, sorting 1 million exam scores in the range 0-100. It's not practical when the value range is huge (like 32-bit integers) because the counting array would be too large.",
      "difficulty": "medium",
      "tags": [
        "sorting",
        "counting-sort",
        "time-complexity"
      ]
    },
    {
      "id": "dsa-021",
      "categoryId": "dsa",
      "question": "What is topological sort and where is it used?",
      "answer": "Topological sort linearly orders vertices of a DAG so that for every edge u→v, u comes before v. It's used for dependency resolution — build systems (Makefile), course prerequisites, task scheduling. Implement it with Kahn's algorithm (repeatedly remove nodes with in-degree 0) or DFS (add node to result in post-order, then reverse). Both run in O(V + E). It only works on DAGs; if the graph has a cycle, no valid ordering exists.",
      "difficulty": "medium",
      "tags": [
        "graphs",
        "topological-sort",
        "dag"
      ]
    },
    {
      "id": "dsa-022",
      "categoryId": "dsa",
      "question": "How would you find the k-th largest element in an unsorted array? What's the most efficient approach?",
      "answer": "The optimal approach is Quickselect, which partitions the array like quicksort but only recurses into one side. Average time is O(n), worst case O(n²), but using median-of-medians pivot selection guarantees O(n). Alternatively, use a min-heap of size k — iterate through the array, push each element, and pop when heap size exceeds k. This gives O(n log k), which is better when k is much smaller than n.",
      "difficulty": "medium",
      "tags": [
        "arrays",
        "heaps",
        "quickselect",
        "sorting"
      ]
    },
    {
      "id": "dsa-023",
      "categoryId": "dsa",
      "question": "Explain what amortized O(1) means in the context of dynamic array insertions.",
      "answer": "Amortized O(1) means that while a single operation might occasionally be expensive, the average cost per operation over a sequence of operations is constant. For a dynamic array, most appends are O(1), but when the array is full, it doubles in size and copies all elements — an O(n) operation. Since doubling happens after n insertions, the total cost for n inserts is roughly 3n, giving O(1) amortized per insert. This is why ArrayList and Python lists are efficient despite occasional resizing.",
      "difficulty": "easy",
      "tags": [
        "arrays",
        "amortized-analysis",
        "time-complexity"
      ]
    },
    {
      "id": "dsa-024",
      "categoryId": "dsa",
      "question": "How would you design an LRU cache with O(1) get and put operations?",
      "answer": "Use a hash map for O(1) key lookup combined with a doubly linked list to maintain access order. On get: move the node to the head of the list. On put: insert at the head; if capacity is exceeded, evict the tail node and remove its key from the map. The hash map stores key → node pointer, so both moving to head and eviction are O(1). This is exactly how Java's LinkedHashMap works with access-order mode enabled.",
      "difficulty": "hard",
      "tags": [
        "hash-maps",
        "linked-lists",
        "cache",
        "design"
      ]
    },
    {
      "id": "dsa-025",
      "categoryId": "dsa",
      "question": "What is the difference between O(n log n) and O(n²) in practice? At what input size does it start to matter?",
      "answer": "At n=1,000, O(n log n) does about 10,000 operations while O(n²) does 1,000,000 — a 100x difference. At n=1,000,000, it becomes 20 million vs 1 trillion, which is the difference between milliseconds and hours. In competitive programming, the rule of thumb is that O(n²) becomes too slow around n=10,000 for a 1-second time limit. In production, even at n=1,000, the quadratic solution may cause noticeable latency under load.",
      "difficulty": "easy",
      "tags": [
        "time-complexity",
        "big-o",
        "performance"
      ]
    },
    {
      "id": "dsa-026",
      "categoryId": "dsa",
      "question": "How do you find the lowest common ancestor (LCA) of two nodes in a binary tree?",
      "answer": "For a general binary tree, recursively check left and right subtrees. If both sides return non-null, the current node is the LCA. If only one side returns non-null, propagate that result up. This runs in O(n) time, O(h) space. For a BST, you can exploit the ordering property: if both nodes are smaller, go left; if both larger, go right; otherwise the current node is the LCA, running in O(h). For repeated queries, preprocess with binary lifting for O(log n) per query.",
      "difficulty": "hard",
      "tags": [
        "trees",
        "bst",
        "recursion",
        "lca"
      ]
    },
    {
      "id": "dsa-027",
      "categoryId": "dsa",
      "question": "Explain how you would serialize and deserialize a binary tree.",
      "answer": "Use preorder traversal, recording null children as a sentinel value (like '#'). Serialize: visit root, recurse left, recurse right, outputting values separated by commas with '#' for nulls. Deserialize: read tokens sequentially, create a node for each non-null token, and recursively build left then right subtrees. Both operations are O(n). This approach works for any binary tree, not just BSTs, because the null markers preserve the exact structure.",
      "difficulty": "hard",
      "tags": [
        "trees",
        "serialization",
        "recursion"
      ]
    },
    {
      "id": "dsa-028",
      "categoryId": "dsa",
      "question": "What is the difference between tail recursion and regular recursion? Why does it matter?",
      "answer": "In tail recursion, the recursive call is the very last operation — no computation happens after it returns. This allows compilers to optimize it into a loop (tail call optimization), reusing the current stack frame and making it O(1) space instead of O(n). Regular recursion accumulates stack frames because it needs to do work after the recursive call returns. Languages like Scheme guarantee TCO; others like Python and Java do not, so deep recursion risks stack overflow.",
      "difficulty": "medium",
      "tags": [
        "recursion",
        "tail-recursion",
        "optimization"
      ]
    },
    {
      "id": "dsa-029",
      "categoryId": "dsa",
      "question": "How would you find all permutations of a string or array? What is the time complexity?",
      "answer": "Use backtracking: for each position, swap it with every subsequent element, recurse on the remaining positions, then swap back. For n distinct elements, there are n! permutations, each of length n, so time complexity is O(n × n!). If there are duplicates, sort first and skip swapping when the current element equals the previous one at the same level. Space is O(n) for the recursion stack plus O(n × n!) to store results.",
      "difficulty": "medium",
      "tags": [
        "recursion",
        "backtracking",
        "permutations"
      ]
    },
    {
      "id": "dsa-030",
      "categoryId": "dsa",
      "question": "How does a union-find (disjoint set) data structure work, and what are its key optimizations?",
      "answer": "Union-find tracks elements partitioned into disjoint sets with two operations: find (which set does x belong to) and union (merge two sets). The two critical optimizations are path compression (during find, make every node point directly to the root) and union by rank (attach the shorter tree under the taller one). Together, these give nearly O(1) amortized time per operation — technically O(α(n)) where α is the inverse Ackermann function, which is ≤ 4 for any practical input. It's used in Kruskal's MST and connected components.",
      "difficulty": "hard",
      "tags": [
        "union-find",
        "graphs",
        "data-structures"
      ]
    },
    {
      "id": "dsa-031",
      "categoryId": "dsa",
      "question": "What is the maximum subarray problem and how do you solve it optimally?",
      "answer": "Given an array of integers, find the contiguous subarray with the maximum sum. Kadane's algorithm solves it in O(n) time, O(1) space: maintain a running sum, and at each element, either extend the current subarray or start fresh (currentSum = max(nums[i], currentSum + nums[i])). Track the global maximum as you iterate. For example, in [-2, 1, -3, 4, -1, 2, 1, -5, 4], the answer is [4, -1, 2, 1] with sum 6.",
      "difficulty": "easy",
      "tags": [
        "dynamic-programming",
        "arrays",
        "kadanes-algorithm"
      ]
    },
    {
      "id": "dsa-032",
      "categoryId": "dsa",
      "question": "How would you implement a graph using an adjacency list vs adjacency matrix? What are the trade-offs?",
      "answer": "An adjacency list stores a list of neighbors per vertex — it uses O(V + E) space and is efficient for sparse graphs. An adjacency matrix uses a V×V boolean array — O(V²) space, but edge existence checks are O(1) vs O(degree) for adjacency lists. Use a matrix when the graph is dense (E ≈ V²) or you need frequent edge lookups. Use a list for sparse graphs (most real-world networks) where V² memory is prohibitive.",
      "difficulty": "easy",
      "tags": [
        "graphs",
        "adjacency-list",
        "adjacency-matrix"
      ]
    },
    {
      "id": "dsa-033",
      "categoryId": "dsa",
      "question": "Explain the coin change problem and how to solve it with dynamic programming.",
      "answer": "Given coin denominations and a target amount, find the minimum number of coins needed. Define dp[i] as the minimum coins for amount i. Initialize dp[0] = 0, all others to infinity. For each amount from 1 to target, for each coin, dp[i] = min(dp[i], dp[i - coin] + 1) if i >= coin. Time is O(amount × coins), space O(amount). For example, with coins [1, 5, 10] and amount 12, the answer is 3 (10 + 1 + 1). Greedy fails here for some denominations — e.g., coins [1, 3, 4] and amount 6, greedy gives 4+1+1=3 coins but optimal is 3+3=2.",
      "difficulty": "medium",
      "tags": [
        "dynamic-programming",
        "coin-change",
        "greedy"
      ]
    },
    {
      "id": "dsa-034",
      "categoryId": "dsa",
      "question": "What is a monotonic stack and when would you use one?",
      "answer": "A monotonic stack maintains elements in strictly increasing or decreasing order. When a new element violates the monotonic property, you pop elements until the invariant is restored. It's used to solve 'next greater element,' 'largest rectangle in histogram,' and 'trapping rain water' in O(n) time. For example, to find the next greater element for each array value, maintain a decreasing stack — when you push a larger element, pop and record it as the answer for all popped elements.",
      "difficulty": "hard",
      "tags": [
        "stacks",
        "monotonic-stack",
        "arrays"
      ]
    },
    {
      "id": "dsa-035",
      "categoryId": "dsa",
      "question": "How do you detect if a linked list is a palindrome in O(n) time and O(1) space?",
      "answer": "Use the slow/fast pointer technique to find the midpoint. Reverse the second half of the list in-place. Compare the first half with the reversed second half node by node. If all values match, it's a palindrome. Optionally, reverse the second half again to restore the original list. This is O(n) time and O(1) space because you only use pointer manipulation, no extra data structure. The key insight is combining three techniques: finding the middle, reversing a linked list, and two-pointer comparison.",
      "difficulty": "hard",
      "tags": [
        "linked-lists",
        "two-pointers",
        "palindrome"
      ]
    },
    {
      "id": "sd-001",
      "categoryId": "system-design",
      "question": "What is a load balancer and what are the common load balancing strategies?",
      "answer": "A load balancer distributes incoming traffic across multiple servers to prevent any single server from becoming a bottleneck. Common strategies include round-robin (simple rotation), least connections (route to the server with fewest active connections), weighted round-robin (servers with more capacity get more requests), and IP hash (consistent routing for sticky sessions). L4 balancers operate at the transport layer (TCP), while L7 balancers can inspect HTTP headers and route based on URL path or cookies.",
      "difficulty": "easy",
      "tags": [
        "load-balancing",
        "scalability",
        "networking"
      ]
    },
    {
      "id": "sd-002",
      "categoryId": "system-design",
      "question": "Explain the different layers of caching in a web application and when you'd use each.",
      "answer": "From closest to the user outward: browser cache (static assets via Cache-Control headers), CDN cache (geographically distributed edge caching for static and semi-static content), application-level cache (Redis or Memcached for computed results, session data, database query results), and database query cache. The rule of thumb is cache at the highest layer possible — CDN for static content, Redis for hot database queries. Cache invalidation is the hard part: use TTL for eventual consistency or write-through/write-behind for stronger consistency.",
      "difficulty": "medium",
      "tags": [
        "caching",
        "redis",
        "cdn",
        "performance"
      ]
    },
    {
      "id": "sd-003",
      "categoryId": "system-design",
      "question": "What is database sharding and what are its challenges?",
      "answer": "Sharding horizontally partitions data across multiple database instances. For example, users with ID 1-1M on shard 1, 1M-2M on shard 2. It improves write throughput and storage capacity beyond what a single machine can handle. Key challenges: cross-shard queries are expensive (joins across shards), rebalancing data when adding shards is complex, maintaining referential integrity is hard, and choosing the wrong shard key can create hot spots. Many teams delay sharding by scaling reads with replicas and using caching first.",
      "difficulty": "hard",
      "tags": [
        "database-sharding",
        "scalability",
        "databases"
      ]
    },
    {
      "id": "sd-004",
      "categoryId": "system-design",
      "question": "Explain the CAP theorem and how it applies to real-world system choices.",
      "answer": "CAP states that a distributed system can guarantee at most two of three properties: Consistency (all reads see the latest write), Availability (every request gets a response), and Partition tolerance (system works despite network splits). Since partitions are inevitable in distributed systems, the real choice is between CP and AP. DynamoDB and Cassandra choose AP (eventual consistency, always available). PostgreSQL with synchronous replication and etcd choose CP (strong consistency, may reject requests during partitions).",
      "difficulty": "medium",
      "tags": [
        "cap-theorem",
        "distributed-systems",
        "consistency"
      ]
    },
    {
      "id": "sd-005",
      "categoryId": "system-design",
      "question": "What are the trade-offs between microservices and a monolith architecture?",
      "answer": "Monoliths are simpler to develop, test, deploy, and debug — one codebase, one deployment, no network calls between components. Microservices enable independent scaling and deployment of services, technology diversity, and team autonomy, but introduce network latency, distributed transaction complexity, service discovery, and operational overhead (monitoring, tracing, CI/CD per service). Start with a monolith until you have clear scaling bottlenecks or team coordination problems. Companies like Shopify run massive monoliths successfully; premature microservices are a common antipattern.",
      "difficulty": "medium",
      "tags": [
        "microservices",
        "monolith",
        "architecture",
        "trade-offs"
      ]
    },
    {
      "id": "sd-006",
      "categoryId": "system-design",
      "question": "How does Kafka work as a message queue and when would you choose it over RabbitMQ?",
      "answer": "Kafka is a distributed commit log where producers append messages to partitioned topics and consumers pull messages at their own pace, tracking offsets. Messages are persisted to disk and retained for a configurable period. Choose Kafka for high-throughput event streaming (millions of messages/sec), event sourcing, log aggregation, and when consumers need to replay messages. Choose RabbitMQ for traditional task queues with complex routing, lower latency per-message delivery, and when you need features like priority queues and dead-letter exchanges.",
      "difficulty": "medium",
      "tags": [
        "message-queues",
        "kafka",
        "rabbitmq",
        "event-streaming"
      ]
    },
    {
      "id": "sd-007",
      "categoryId": "system-design",
      "question": "What is an API gateway and what responsibilities does it handle?",
      "answer": "An API gateway is a single entry point for client requests that routes them to appropriate backend services. It handles cross-cutting concerns: authentication/authorization, rate limiting, request/response transformation, TLS termination, load balancing, caching, and request logging. It also enables API versioning and can aggregate responses from multiple services into one. Examples include Kong, AWS API Gateway, and NGINX. The trade-off is that it becomes a single point of failure, so it must be highly available and horizontally scalable itself.",
      "difficulty": "easy",
      "tags": [
        "api-gateway",
        "microservices",
        "networking"
      ]
    },
    {
      "id": "sd-008",
      "categoryId": "system-design",
      "question": "How would you implement rate limiting at scale? Describe the algorithms.",
      "answer": "Common algorithms: token bucket (tokens refill at a fixed rate, each request consumes one; allows bursts up to bucket size), sliding window log (store timestamps of requests, count within window), and sliding window counter (hybrid of fixed window counters with weighted overlap). At scale, use a centralized store like Redis with atomic operations — INCR with TTL for fixed windows, or sorted sets for sliding window logs. For distributed systems, either use a shared Redis cluster or accept approximate limiting with local counters that periodically sync.",
      "difficulty": "hard",
      "tags": [
        "rate-limiting",
        "redis",
        "algorithms",
        "scalability"
      ]
    },
    {
      "id": "sd-009",
      "categoryId": "system-design",
      "question": "What is consistent hashing and why is it important for distributed systems?",
      "answer": "Consistent hashing maps both keys and servers onto a hash ring. Each key is assigned to the next server clockwise on the ring. When a server is added or removed, only K/N keys need to be remapped (K = total keys, N = total servers), compared to rehashing everything in traditional modular hashing. Virtual nodes (multiple points per server on the ring) improve balance. It's used in DynamoDB, Cassandra, CDNs, and distributed caches to minimize data movement during scaling events.",
      "difficulty": "hard",
      "tags": [
        "consistent-hashing",
        "distributed-systems",
        "scalability"
      ]
    },
    {
      "id": "sd-010",
      "categoryId": "system-design",
      "question": "Design a URL shortener like bit.ly. Walk me through the high-level architecture.",
      "answer": "Core flow: user submits a long URL, service generates a unique short code (6-7 chars from base62 gives 62^7 = 3.5 trillion combinations), stores the mapping in a database, and returns the short URL. On redirect, look up the short code, return a 301/302 redirect. Use a distributed ID generator (snowflake IDs or pre-generated ID ranges) to avoid collisions. Add a Redis cache in front of the database since reads vastly outnumber writes (read-heavy). For analytics, log redirect events to Kafka and process asynchronously. Shard the database by short code hash for horizontal scaling.",
      "difficulty": "medium",
      "tags": [
        "url-shortener",
        "system-design",
        "caching",
        "databases"
      ]
    },
    {
      "id": "sd-011",
      "categoryId": "system-design",
      "question": "How would you design a real-time chat system like WhatsApp or Slack?",
      "answer": "Users connect via persistent WebSocket connections to chat servers. When user A sends a message, the chat server publishes it to a message queue (Kafka) keyed by conversation ID. The recipient's chat server consumes the message and pushes it over their WebSocket. For offline users, store undelivered messages in a database and deliver on reconnect. Use a presence service (heartbeats via Redis with TTL) to track online status. Messages are stored in a database like Cassandra partitioned by conversation ID and sorted by timestamp. Group chats fan out messages to all participants.",
      "difficulty": "hard",
      "tags": [
        "chat-system",
        "websockets",
        "real-time",
        "message-queues"
      ]
    },
    {
      "id": "sd-012",
      "categoryId": "system-design",
      "question": "Design a notification system that handles push, SMS, and email at scale.",
      "answer": "A notification service receives events from various internal services, determines the notification type and user preferences, then routes to the appropriate channel (push/SMS/email). Use a message queue (Kafka or SQS) between the notification service and channel-specific workers to decouple and buffer. Each channel worker handles vendor-specific delivery (APNs for iOS push, FCM for Android, Twilio for SMS, SES for email). Include a deduplication layer (check against Redis with message ID), rate limiting per user, and a priority queue for urgent vs. marketing notifications. Store notification history in a database for user's notification center.",
      "difficulty": "hard",
      "tags": [
        "notification-system",
        "message-queues",
        "scalability"
      ]
    },
    {
      "id": "sd-013",
      "categoryId": "system-design",
      "question": "How does a CDN work and how would you design one?",
      "answer": "A CDN caches content at edge servers geographically close to users, reducing latency and origin server load. When a user requests content, DNS routes them to the nearest edge (anycast or GeoDNS). If the edge has the content (cache hit), it serves directly. On a cache miss, the edge fetches from the origin, caches it, and serves. Design considerations: cache invalidation strategy (TTL + purge API), tiered caching (edge → regional → origin), consistent hashing for cache distribution, TLS termination at edges, and DDoS protection. Major CDNs like Cloudflare have 300+ PoPs worldwide.",
      "difficulty": "medium",
      "tags": [
        "cdn",
        "caching",
        "networking",
        "scalability"
      ]
    },
    {
      "id": "sd-014",
      "categoryId": "system-design",
      "question": "What are the different database replication strategies and their trade-offs?",
      "answer": "Single-leader (master-slave): one node handles writes, replicas handle reads. Simple but the leader is a write bottleneck and single point of failure. Multi-leader: multiple nodes accept writes, sync with each other — better write availability but introduces conflict resolution complexity. Leaderless (Dynamo-style): reads and writes go to multiple nodes, using quorum (W + R > N) for consistency. Synchronous replication guarantees consistency but increases write latency; asynchronous is faster but risks data loss if the leader fails before replication completes.",
      "difficulty": "medium",
      "tags": [
        "database-replication",
        "distributed-systems",
        "consistency"
      ]
    },
    {
      "id": "sd-015",
      "categoryId": "system-design",
      "question": "When should you scale horizontally vs vertically? What are the limits of each?",
      "answer": "Vertical scaling (bigger machine) is simpler — no code changes, no distributed system complexity. It hits a ceiling: the largest AWS instance has 24 TB RAM and 448 vCPUs, and there's a single point of failure. Horizontal scaling (more machines) is theoretically unlimited but requires your application to handle distributed state, load balancing, and data partitioning. Scale vertically first for databases (it's dramatically simpler), and horizontally for stateless application servers. Most systems use both: vertically-scaled databases with read replicas, and horizontally-scaled app tiers.",
      "difficulty": "easy",
      "tags": [
        "horizontal-scaling",
        "vertical-scaling",
        "scalability"
      ]
    },
    {
      "id": "sd-016",
      "categoryId": "system-design",
      "question": "What is event-driven architecture and when is it the right choice?",
      "answer": "In event-driven architecture, services communicate by producing and consuming events (facts about things that happened) rather than direct synchronous calls. A user placing an order publishes an OrderPlaced event; inventory, billing, and shipping services independently react. Benefits: loose coupling, independent scalability, natural audit log, and resilience (if billing is down, the event waits in the queue). Downsides: eventual consistency, harder debugging (no single request trace), event schema evolution, and potential event ordering issues. It's the right choice for workflows with multiple downstream consumers and where temporary inconsistency is acceptable.",
      "difficulty": "medium",
      "tags": [
        "event-driven",
        "architecture",
        "message-queues",
        "decoupling"
      ]
    },
    {
      "id": "sd-017",
      "categoryId": "system-design",
      "question": "What is CQRS and when would you use it?",
      "answer": "CQRS (Command Query Responsibility Segregation) separates the write model (commands) from the read model (queries), potentially using different databases optimized for each. Writes go to a normalized relational store, while reads come from denormalized views (materialized in Elasticsearch, Redis, or a read-optimized database). It's useful when read and write patterns differ significantly — for example, an e-commerce product page needs fast denormalized reads, but inventory updates need transactional writes. The trade-off is complexity: you need a synchronization mechanism (usually events) between write and read stores, and reads become eventually consistent.",
      "difficulty": "hard",
      "tags": [
        "cqrs",
        "architecture",
        "databases",
        "event-driven"
      ]
    },
    {
      "id": "sd-018",
      "categoryId": "system-design",
      "question": "How would you design a distributed rate limiter that works across multiple servers?",
      "answer": "The challenge is that each server sees only a fraction of requests, so local counters undercount. Solution: use a centralized store like Redis. For a sliding window with Redis: use a sorted set per user, add each request timestamp, remove entries outside the window, and check the count — all in a single Lua script for atomicity. For higher throughput, use a token bucket in Redis with two keys (token count and last refill timestamp). To handle Redis being a single point of failure, run Redis Cluster. For approximate limiting at extreme scale, use local counters that sync periodically, accepting some over-admission.",
      "difficulty": "hard",
      "tags": [
        "rate-limiting",
        "distributed-systems",
        "redis"
      ]
    },
    {
      "id": "sd-019",
      "categoryId": "system-design",
      "question": "What is the difference between a reverse proxy and a load balancer?",
      "answer": "A reverse proxy sits in front of backend servers and handles tasks like SSL termination, compression, caching, and security (hiding backend topology). A load balancer specifically distributes traffic across multiple servers for availability and throughput. In practice, most reverse proxies (NGINX, HAProxy) also do load balancing, and most load balancers do reverse proxy functions — the terms overlap heavily. The key distinction is intent: use 'reverse proxy' when the focus is on security and request handling, and 'load balancer' when the focus is on distributing load across replicas.",
      "difficulty": "easy",
      "tags": [
        "reverse-proxy",
        "load-balancing",
        "networking"
      ]
    },
    {
      "id": "sd-020",
      "categoryId": "system-design",
      "question": "How would you handle distributed transactions across microservices?",
      "answer": "Avoid them if possible — they're complex and slow. When necessary, use the Saga pattern: a sequence of local transactions where each step publishes an event triggering the next, with compensating transactions for rollback. For example, an order saga: reserve inventory → charge payment → ship. If payment fails, the compensating action releases the inventory reservation. There are two flavors: choreography (services react to events) and orchestration (a central coordinator directs the flow). The two-phase commit (2PC) protocol provides stronger consistency but is slow and blocks resources, so it's rarely used across services.",
      "difficulty": "hard",
      "tags": [
        "distributed-transactions",
        "sagas",
        "microservices"
      ]
    },
    {
      "id": "sd-021",
      "categoryId": "system-design",
      "question": "Walk me through how you'd design a web crawler that can scale to billions of pages.",
      "answer": "Use a URL frontier (priority queue of URLs to crawl) backed by a distributed queue like Kafka. Worker nodes dequeue URLs, respect robots.txt (cache it per domain), fetch pages with politeness delays (1-2 seconds per domain), extract links, and enqueue new ones. Deduplicate URLs with a Bloom filter or a distributed set in Redis. Store raw HTML in blob storage (S3) and parsed content in a search index. Key challenges: politeness (don't DDoS sites), deduplication at scale, handling dynamic JavaScript content (headless browsers are 10x slower), prioritizing important pages, and detecting spider traps (infinite URL patterns).",
      "difficulty": "hard",
      "tags": [
        "web-crawler",
        "distributed-systems",
        "scalability"
      ]
    },
    {
      "id": "sd-022",
      "categoryId": "system-design",
      "question": "What is database connection pooling and why is it essential at scale?",
      "answer": "Opening a database connection involves TCP handshake, TLS negotiation, and authentication — typically 20-50ms. Connection pooling maintains a set of pre-established connections that are reused across requests. A pool of 20 connections can serve thousands of requests per second since most queries take only 1-5ms. Without pooling, a spike of 1,000 concurrent requests would try to open 1,000 connections, overwhelming the database (PostgreSQL forks a process per connection). Tools like PgBouncer sit between the application and database, multiplexing thousands of client connections onto a smaller pool.",
      "difficulty": "easy",
      "tags": [
        "databases",
        "connection-pooling",
        "performance"
      ]
    },
    {
      "id": "sd-023",
      "categoryId": "system-design",
      "question": "How do you decide between SQL and NoSQL databases for a given system?",
      "answer": "Choose SQL (PostgreSQL, MySQL) when you need ACID transactions, complex joins, strong consistency, and your data is relational with a well-defined schema. Choose NoSQL when you need horizontal scalability, flexible schemas, or your access patterns are key-value (DynamoDB, Redis), document-based (MongoDB), wide-column (Cassandra), or graph-based (Neo4j). The practical heuristic: start with PostgreSQL unless you have a specific reason not to — it handles JSON documents, full-text search, and scales vertically to very large datasets. Only move to NoSQL when you've hit clear relational limits at scale.",
      "difficulty": "easy",
      "tags": [
        "databases",
        "sql",
        "nosql",
        "trade-offs"
      ]
    },
    {
      "id": "sd-024",
      "categoryId": "system-design",
      "question": "How does an idempotency key work and why is it critical in payment systems?",
      "answer": "An idempotency key is a unique token (usually a UUID) that the client sends with each request. The server stores the key and its result; if the same key is received again, it returns the stored result instead of re-executing the operation. This prevents duplicate charges when a client retries after a network timeout — did my payment go through or not? Implementation: store idempotency keys in Redis with a TTL (e.g., 24 hours). Before processing, check if the key exists; if so, return the cached response. Use a database transaction to atomically check-and-insert the key alongside the payment to prevent race conditions.",
      "difficulty": "medium",
      "tags": [
        "idempotency",
        "payments",
        "distributed-systems",
        "reliability"
      ]
    },
    {
      "id": "sd-025",
      "categoryId": "system-design",
      "question": "How would you design a system to handle 10 million concurrent WebSocket connections?",
      "answer": "A single server can handle roughly 100K-500K concurrent WebSocket connections (limited by memory — each connection uses ~10-50KB). For 10M connections, you need 50-100 servers behind a L4 load balancer that supports sticky sessions (since WebSocket is stateful). For cross-server messaging, use a pub/sub layer: Redis Pub/Sub for moderate scale, or Kafka for higher throughput. When user A on server 1 sends a message to user B on server 47, publish to a channel that server 47 subscribes to. Track user-to-server mapping in Redis. Use heartbeats to detect dead connections and free memory. Horizontal scaling is linear — just add more WebSocket servers behind the load balancer.",
      "difficulty": "hard",
      "tags": [
        "websockets",
        "scalability",
        "real-time",
        "distributed-systems"
      ]
    },
    {
      "id": "web-001",
      "categoryId": "web-frontend",
      "question": "What is the DOM and how does the browser construct it?",
      "answer": "The DOM (Document Object Model) is a tree-structured representation of an HTML document that the browser builds by parsing HTML tokens into nodes. The browser fetches HTML, tokenizes it, builds the DOM tree, then combines it with the CSSOM to create the render tree. JavaScript can manipulate this tree via APIs like document.querySelector() and element.appendChild(), which can trigger reflows and repaints.",
      "difficulty": "easy",
      "tags": [
        "dom",
        "browser",
        "rendering"
      ]
    },
    {
      "id": "web-002",
      "categoryId": "web-frontend",
      "question": "How does React's virtual DOM improve performance compared to direct DOM manipulation?",
      "answer": "React maintains a lightweight JavaScript copy of the real DOM. When state changes, React builds a new virtual DOM tree, diffs it against the previous one (reconciliation), and computes the minimal set of real DOM mutations needed. This batches updates and avoids expensive redundant reflows. The key insight is that JavaScript object comparison is fast while real DOM operations are slow, so the diffing overhead is worth it.",
      "difficulty": "medium",
      "tags": [
        "react",
        "virtual-dom",
        "performance"
      ]
    },
    {
      "id": "web-003",
      "categoryId": "web-frontend",
      "question": "Explain the CSS box model. What is the difference between content-box and border-box?",
      "answer": "Every CSS element is a rectangular box with content, padding, border, and margin layers. In content-box (the default), width/height only set the content area — padding and border are added on top, so a 200px-wide element with 20px padding is actually 240px wide. In border-box, width/height include padding and border, so that same element stays at 200px total. Most teams set * { box-sizing: border-box } globally to make layouts predictable.",
      "difficulty": "easy",
      "tags": [
        "css",
        "box-model",
        "layout"
      ]
    },
    {
      "id": "web-004",
      "categoryId": "web-frontend",
      "question": "When would you use CSS Grid vs Flexbox?",
      "answer": "Flexbox is one-dimensional — it excels at laying out items in a single row or column, like a navbar or a card's internal layout. Grid is two-dimensional — it handles rows and columns simultaneously, making it ideal for page-level layouts and dashboards. A practical rule: use Grid for the page skeleton and Flexbox for component internals. They compose well together; a Grid cell's children can be a flex container.",
      "difficulty": "easy",
      "tags": [
        "css",
        "flexbox",
        "grid",
        "layout"
      ]
    },
    {
      "id": "web-005",
      "categoryId": "web-frontend",
      "question": "What is event delegation and why is it useful?",
      "answer": "Event delegation leverages DOM event bubbling by attaching a single event listener to a parent element instead of individual listeners on each child. When a child is clicked, the event bubbles up to the parent where you check event.target to determine which child was clicked. This is more memory-efficient for large lists (one listener vs thousands), automatically handles dynamically added elements, and is how React's synthetic event system works internally.",
      "difficulty": "medium",
      "tags": [
        "dom",
        "events",
        "event-delegation"
      ]
    },
    {
      "id": "web-006",
      "categoryId": "web-frontend",
      "question": "Explain closures in JavaScript with a practical example.",
      "answer": "A closure is a function that retains access to its enclosing scope's variables even after that outer function has returned. For example, function makeCounter() { let count = 0; return () => ++count; } — calling makeCounter() returns a function that remembers and mutates count. Closures are fundamental to module patterns, data privacy, callbacks, and React hooks. A classic pitfall is closures inside loops capturing the same variable reference, which is solved with let (block scoping) or IIFE wrappers.",
      "difficulty": "medium",
      "tags": [
        "javascript",
        "closures",
        "scope"
      ]
    },
    {
      "id": "web-007",
      "categoryId": "web-frontend",
      "question": "What is the difference between Promises and async/await? When would you use one over the other?",
      "answer": "Async/await is syntactic sugar over Promises — they compile to the same thing. Promises use .then()/.catch() chaining, while async/await lets you write asynchronous code that reads like synchronous code. Use async/await for sequential flows where readability matters. Use raw Promises when you need Promise.all() for parallel execution, Promise.race() for timeouts, or Promise.allSettled() when some failures are acceptable. You can mix them freely since async functions return Promises.",
      "difficulty": "medium",
      "tags": [
        "javascript",
        "promises",
        "async-await"
      ]
    },
    {
      "id": "web-008",
      "categoryId": "web-frontend",
      "question": "What is CORS and how do you handle it?",
      "answer": "CORS (Cross-Origin Resource Sharing) is a browser security mechanism that blocks requests from one origin (protocol+domain+port) to a different origin unless the server explicitly allows it via headers. The server must respond with Access-Control-Allow-Origin, and for non-simple requests (PUT, DELETE, custom headers), the browser sends a preflight OPTIONS request first. Fix it server-side by setting appropriate CORS headers, not by disabling it client-side. In development, a proxy in your dev server config avoids CORS entirely.",
      "difficulty": "medium",
      "tags": [
        "cors",
        "security",
        "http"
      ]
    },
    {
      "id": "web-009",
      "categoryId": "web-frontend",
      "question": "Walk me through the HTTP methods and when you'd use each one.",
      "answer": "GET retrieves resources and must be idempotent and cacheable. POST creates new resources or triggers actions and is not idempotent. PUT replaces an entire resource (idempotent). PATCH partially updates a resource. DELETE removes a resource (idempotent). HEAD is identical to GET but returns only headers, useful for checking if a resource exists. OPTIONS describes available methods, used in CORS preflight. The key interview point: idempotency means calling the same request multiple times produces the same result.",
      "difficulty": "easy",
      "tags": [
        "http",
        "rest",
        "api-design"
      ]
    },
    {
      "id": "web-010",
      "categoryId": "web-frontend",
      "question": "Compare REST and GraphQL. When would you choose each?",
      "answer": "REST exposes fixed endpoints per resource (/users, /users/1/posts) while GraphQL exposes a single endpoint where clients specify exactly what data they need in a query. REST leads to over-fetching (getting fields you don't need) or under-fetching (requiring multiple requests), which GraphQL solves with its query language. Choose REST for simple CRUD APIs, public APIs, and when caching via HTTP is important. Choose GraphQL for complex UIs with varied data needs, mobile apps sensitive to payload size, or when frontend teams need to iterate without backend changes.",
      "difficulty": "medium",
      "tags": [
        "rest",
        "graphql",
        "api-design"
      ]
    },
    {
      "id": "web-011",
      "categoryId": "web-frontend",
      "question": "How would you prevent XSS attacks in a web application?",
      "answer": "XSS (Cross-Site Scripting) injects malicious scripts into pages viewed by other users. Prevent it by always escaping output — frameworks like React auto-escape JSX by default. Never use innerHTML or dangerouslySetInnerHTML with user input. Set Content-Security-Policy headers to restrict script sources. Use httpOnly cookies so scripts cannot steal session tokens. Sanitize any HTML you must render using a library like DOMPurify. The key principle is never trust user input and always encode for the output context (HTML, URL, JS, CSS).",
      "difficulty": "hard",
      "tags": [
        "security",
        "xss",
        "web-security"
      ]
    },
    {
      "id": "web-012",
      "categoryId": "web-frontend",
      "question": "What is CSRF and how do you prevent it?",
      "answer": "CSRF (Cross-Site Request Forgery) tricks an authenticated user's browser into making unintended requests to a site where they're logged in — for example, a hidden form on a malicious page that POSTs to your bank. Prevent it with anti-CSRF tokens: the server embeds a unique token in each form that the attacker cannot guess. Also use SameSite cookie attributes (Lax or Strict) to prevent cookies from being sent on cross-origin requests. Requiring re-authentication for sensitive actions adds defense in depth.",
      "difficulty": "hard",
      "tags": [
        "security",
        "csrf",
        "web-security"
      ]
    },
    {
      "id": "web-013",
      "categoryId": "web-frontend",
      "question": "What are the key ARIA roles and practices for web accessibility?",
      "answer": "ARIA (Accessible Rich Internet Applications) attributes make dynamic content accessible to screen readers. Key practices: use semantic HTML first (button, nav, main) before reaching for ARIA. Add role attributes (role='dialog', role='alert') for custom components. Use aria-label for elements without visible text, aria-live for dynamic content announcements, and aria-expanded for toggleable sections. Every interactive element must be keyboard-navigable. Run Lighthouse accessibility audits and test with a real screen reader — automated tools catch only about 30% of issues.",
      "difficulty": "medium",
      "tags": [
        "accessibility",
        "aria",
        "a11y"
      ]
    },
    {
      "id": "web-014",
      "categoryId": "web-frontend",
      "question": "What strategies would you use to optimize the performance of a web application?",
      "answer": "Start with the critical rendering path: minimize render-blocking CSS and defer non-critical JS. Use code splitting and lazy loading to reduce initial bundle size. Optimize images with modern formats (WebP/AVIF), responsive srcset, and lazy loading. Enable HTTP/2 for multiplexing, set proper cache headers, and use a CDN. On the runtime side: debounce expensive handlers, virtualize long lists, avoid layout thrashing by batching DOM reads then writes. Measure with Lighthouse and Web Vitals (LCP < 2.5s, FID < 100ms, CLS < 0.1).",
      "difficulty": "hard",
      "tags": [
        "performance",
        "optimization",
        "web-vitals"
      ]
    },
    {
      "id": "web-015",
      "categoryId": "web-frontend",
      "question": "What does webpack (or a modern bundler) actually do?",
      "answer": "A bundler starts from an entry point, builds a dependency graph by following import/require statements, and outputs optimized bundles. Webpack specifically transforms modules via loaders (Babel for JS, css-loader for CSS, file-loader for assets), applies plugins for optimization (tree-shaking, minification, chunk splitting), and supports code splitting via dynamic import(). Modern alternatives like Vite use native ES modules in development for instant HMR and only bundle for production using Rollup. The key interview point is understanding the dependency graph and how tree-shaking eliminates dead code.",
      "difficulty": "medium",
      "tags": [
        "webpack",
        "bundling",
        "build-tools"
      ]
    },
    {
      "id": "web-016",
      "categoryId": "web-frontend",
      "question": "Explain server-side rendering (SSR) vs client-side rendering (CSR). When would you pick each?",
      "answer": "CSR ships a minimal HTML shell and a JS bundle that renders everything in the browser — fast subsequent navigations but slow initial load and poor SEO. SSR generates full HTML on the server per request — fast first contentful paint and excellent SEO but higher server load. Hybrid approaches exist: static site generation (SSG) pre-renders at build time for content that rarely changes, and incremental static regeneration (ISR) in Next.js revalidates on a timer. Pick CSR for dashboards behind auth, SSR/SSG for public-facing, SEO-critical pages.",
      "difficulty": "hard",
      "tags": [
        "ssr",
        "csr",
        "rendering",
        "nextjs"
      ]
    },
    {
      "id": "web-017",
      "categoryId": "web-frontend",
      "question": "What are Web Workers and when would you use them?",
      "answer": "Web Workers run JavaScript in a background thread, separate from the main UI thread, preventing expensive computations from blocking user interactions. Communication happens via postMessage and onmessage using structured cloning (no shared memory by default, though SharedArrayBuffer exists). Use them for CPU-intensive tasks like image processing, parsing large datasets, or cryptographic operations. They cannot access the DOM directly. A practical example: offloading CSV parsing of a 50MB file to a worker so the UI stays responsive.",
      "difficulty": "hard",
      "tags": [
        "web-workers",
        "performance",
        "concurrency"
      ]
    },
    {
      "id": "web-018",
      "categoryId": "web-frontend",
      "question": "What is a Service Worker and how does it enable offline functionality?",
      "answer": "A Service Worker is a programmable proxy that sits between your app and the network, running in a separate thread. It intercepts fetch requests and can serve cached responses when offline. The lifecycle is: register, install (pre-cache critical assets), activate (clean old caches), then fetch (serve from cache or network). Cache strategies include cache-first (fast, may be stale), network-first (fresh, falls back to cache), and stale-while-revalidate (fast and eventually fresh). They also enable push notifications and background sync.",
      "difficulty": "hard",
      "tags": [
        "service-worker",
        "pwa",
        "offline",
        "caching"
      ]
    },
    {
      "id": "web-019",
      "categoryId": "web-frontend",
      "question": "How do CSS media queries work and what is a mobile-first approach?",
      "answer": "Media queries apply CSS rules conditionally based on device characteristics like viewport width, orientation, or prefers-color-scheme. Mobile-first means writing base styles for small screens, then adding min-width breakpoints to enhance for larger screens: @media (min-width: 768px) { ... }. This approach loads less CSS on mobile (where bandwidth matters most), forces you to prioritize content, and progressively enhances. Common breakpoints are 640px (sm), 768px (md), 1024px (lg), and 1280px (xl), though Tailwind and most frameworks let you customize these.",
      "difficulty": "easy",
      "tags": [
        "css",
        "responsive-design",
        "media-queries"
      ]
    },
    {
      "id": "web-020",
      "categoryId": "web-frontend",
      "question": "Compare Redux, Context API, and Zustand for state management in React.",
      "answer": "Context API is built into React and good for low-frequency updates like themes or auth, but causes re-renders in all consumers when any value changes. Redux provides a centralized store with actions and reducers, excellent devtools, middleware (thunks/sagas for async), but adds boilerplate. Zustand is a lightweight alternative (~1KB) with a hooks-based API, no providers needed, selective subscriptions that avoid unnecessary re-renders, and minimal boilerplate. For most apps: use Context for global config, Zustand or Redux Toolkit for complex client state, and React Query/TanStack Query for server state.",
      "difficulty": "medium",
      "tags": [
        "react",
        "state-management",
        "redux",
        "zustand"
      ]
    },
    {
      "id": "web-021",
      "categoryId": "web-frontend",
      "question": "What is the event loop in JavaScript? How do microtasks differ from macrotasks?",
      "answer": "The event loop continuously checks if the call stack is empty, then processes tasks from the queue. Macrotasks include setTimeout, setInterval, and I/O callbacks — one macrotask runs per event loop iteration. Microtasks include Promise.then callbacks and MutationObserver — the entire microtask queue is drained after each macrotask before the browser renders. This means Promise.resolve().then(fn) always runs before setTimeout(fn, 0). Understanding this ordering is essential for debugging async race conditions and knowing when the DOM will actually update.",
      "difficulty": "hard",
      "tags": [
        "javascript",
        "event-loop",
        "async"
      ]
    },
    {
      "id": "web-022",
      "categoryId": "web-frontend",
      "question": "What is the difference between == and === in JavaScript?",
      "answer": "The === operator (strict equality) checks both value and type without coercion — '5' === 5 is false. The == operator (loose equality) performs type coercion before comparison — '5' == 5 is true because the string is converted to a number. The coercion rules for == are notoriously complex (null == undefined is true, but null == 0 is false). Always use === in production code. The one reasonable exception is x == null which catches both null and undefined in a single check.",
      "difficulty": "easy",
      "tags": [
        "javascript",
        "type-coercion",
        "equality"
      ]
    },
    {
      "id": "web-023",
      "categoryId": "web-frontend",
      "question": "How does React's useEffect hook work, and what are common pitfalls?",
      "answer": "useEffect runs side effects after render. With no dependency array, it runs after every render. With an empty array [], it runs once on mount. With dependencies [a, b], it re-runs when those values change (compared by Object.is). The return function is a cleanup that runs before the next effect and on unmount. Common pitfalls: missing dependencies causing stale closures (the exhaustive-deps lint rule catches this), infinite loops from setting state that's also a dependency, and forgetting that effects run after paint (use useLayoutEffect for DOM measurements that must happen before paint).",
      "difficulty": "medium",
      "tags": [
        "react",
        "hooks",
        "useeffect"
      ]
    },
    {
      "id": "web-024",
      "categoryId": "web-frontend",
      "question": "Explain how prototypal inheritance works in JavaScript.",
      "answer": "Every JavaScript object has an internal [[Prototype]] link to another object, forming a prototype chain. When you access a property, the engine walks up the chain until it finds it or reaches null. Constructor functions have a .prototype property that becomes the [[Prototype]] of instances created with new. ES6 classes are syntactic sugar over this mechanism — class extends sets up the prototype chain. Object.create(proto) creates an object with a specified prototype directly. This differs from classical inheritance because objects delegate to other objects rather than being instances of a class blueprint.",
      "difficulty": "medium",
      "tags": [
        "javascript",
        "prototypes",
        "inheritance"
      ]
    },
    {
      "id": "web-025",
      "categoryId": "web-frontend",
      "question": "What is tree shaking and how does it work?",
      "answer": "Tree shaking is a dead code elimination technique that removes unused exports from your final bundle. It works because ES modules have static structure — import/export declarations are analyzed at build time, not runtime. The bundler marks which exports are actually imported, then removes the rest. For it to work, you must use ES module syntax (not CommonJS require), set sideEffects: false in package.json for pure modules, and avoid patterns that defeat static analysis like dynamic imports from barrel files. Webpack, Rollup, and esbuild all support tree shaking, typically saving 20-60% of library code.",
      "difficulty": "medium",
      "tags": [
        "bundling",
        "tree-shaking",
        "optimization"
      ]
    },
    {
      "id": "be-001",
      "categoryId": "backend-apis",
      "question": "What makes a REST API well-designed? Walk me through the key principles.",
      "answer": "A well-designed REST API uses nouns for resources (/users, /orders), HTTP methods for actions (GET reads, POST creates, PUT replaces, PATCH updates, DELETE removes), and standard status codes (200 OK, 201 Created, 404 Not Found, 422 Unprocessable). Use plural resource names, nest related resources logically (/users/123/orders), and keep URLs to a max of 2-3 levels deep. Return consistent response envelopes, support filtering/sorting/pagination via query parameters, and version your API (either URL path /v1/ or Accept header). Always document with OpenAPI/Swagger.",
      "difficulty": "easy",
      "tags": [
        "rest",
        "api-design",
        "http"
      ]
    },
    {
      "id": "be-002",
      "categoryId": "backend-apis",
      "question": "Compare JWT, session-based, and OAuth 2.0 authentication. When would you use each?",
      "answer": "Session-based auth stores state server-side (in Redis or DB) with a session ID in a cookie — simple, easy to revoke, but requires sticky sessions or shared storage in distributed systems. JWT is stateless — the token contains encoded claims signed by the server, scaling easily but hard to revoke before expiry (use short-lived access tokens + refresh tokens). OAuth 2.0 is a delegation framework for third-party access (Login with Google) and is complementary, not a replacement. Use sessions for traditional server-rendered apps, JWT for SPAs and microservices, OAuth when you need third-party identity or authorization.",
      "difficulty": "medium",
      "tags": [
        "authentication",
        "jwt",
        "oauth",
        "sessions"
      ]
    },
    {
      "id": "be-003",
      "categoryId": "backend-apis",
      "question": "What is middleware in a backend framework and how does it work?",
      "answer": "Middleware functions sit in the request-response pipeline and execute sequentially before (or after) the route handler. Each middleware receives the request and response objects and a next() function to pass control to the next middleware. Common uses: authentication checks, request logging, body parsing, CORS headers, rate limiting, and error handling. In Express, app.use(middleware) applies globally while router.use() applies to specific routes. The order of middleware registration matters — an auth middleware must come before protected route handlers. Error-handling middleware takes four arguments (err, req, res, next).",
      "difficulty": "easy",
      "tags": [
        "middleware",
        "express",
        "request-pipeline"
      ]
    },
    {
      "id": "be-004",
      "categoryId": "backend-apis",
      "question": "Why is database connection pooling important and how does it work?",
      "answer": "Opening a database connection is expensive — TCP handshake, TLS negotiation, authentication can take 20-50ms per connection. A connection pool maintains a set of pre-established connections that are checked out for queries and returned when done. This amortizes the connection cost, limits the total connections to the database (preventing overload), and reduces latency. Typical pool sizes follow the formula: connections = (CPU cores * 2) + effective_disk_spindles, so around 10-20 for most servers. In Node.js with pg, you'd use a Pool with max: 20 and idle timeout. Too large a pool actually hurts performance due to context switching.",
      "difficulty": "medium",
      "tags": [
        "database",
        "connection-pooling",
        "performance"
      ]
    },
    {
      "id": "be-005",
      "categoryId": "backend-apis",
      "question": "How should a backend API handle errors? What's your error handling strategy?",
      "answer": "Use a centralized error-handling middleware that catches all thrown errors and maps them to appropriate HTTP status codes and a consistent response format like { error: { code: 'VALIDATION_ERROR', message: '...', details: [...] } }. Distinguish between operational errors (invalid input, not found — return 4xx) and programmer errors (null reference, type errors — return 500 and log/alert). Never expose stack traces or internal details in production responses. Use custom error classes (ValidationError, NotFoundError) that carry their own status codes. Log errors with correlation IDs so you can trace a user's report back to the exact request.",
      "difficulty": "medium",
      "tags": [
        "error-handling",
        "api-design",
        "logging"
      ]
    },
    {
      "id": "be-006",
      "categoryId": "backend-apis",
      "question": "What should you log in a production backend and how do you structure logs?",
      "answer": "Use structured logging (JSON format) with fields: timestamp, level, message, requestId (correlation ID), userId, method, path, statusCode, durationMs, and error details for failures. Log at appropriate levels: ERROR for failures needing attention, WARN for degraded states, INFO for request lifecycle events, DEBUG for development only. Always include a correlation ID propagated across services for distributed tracing. Ship logs to a centralized system (ELK, Datadog, CloudWatch). Never log sensitive data like passwords, tokens, or PII. Log the request and response at the gateway level and business events at the service level.",
      "difficulty": "medium",
      "tags": [
        "logging",
        "observability",
        "production"
      ]
    },
    {
      "id": "be-007",
      "categoryId": "backend-apis",
      "question": "How would you version a REST API?",
      "answer": "The three main approaches are URL path versioning (/api/v1/users — most common, easy to route), query parameter (?version=1 — less common, harder to cache), and header versioning (Accept: application/vnd.myapi.v1+json — technically cleanest but less discoverable). URL path versioning wins in practice because it's visible, cacheable, and easy for consumers to understand. When evolving an API, add new fields as optional to avoid breaking changes. Only bump the version for breaking changes. Support at least the previous version for a deprecation period, and use Sunset headers to signal retirement dates.",
      "difficulty": "easy",
      "tags": [
        "api-versioning",
        "rest",
        "api-design"
      ]
    },
    {
      "id": "be-008",
      "categoryId": "backend-apis",
      "question": "What are the common pagination strategies for APIs?",
      "answer": "Offset-based pagination (GET /items?offset=20&limit=10) is simplest but breaks if data changes between pages and is slow for large offsets (DB must skip N rows). Cursor-based pagination (GET /items?cursor=abc123&limit=10) uses an opaque token encoding the last seen item, which is consistent and performant because the DB can seek directly. Keyset pagination is cursor-based using a real column (e.g., ?after_id=500&limit=10) — very fast with an index on the sort column. Use offset for simple UIs with page numbers, cursor for infinite scroll and real-time feeds. Always return a total count (or has_more flag) and next/prev links.",
      "difficulty": "medium",
      "tags": [
        "pagination",
        "api-design",
        "performance"
      ]
    },
    {
      "id": "be-009",
      "categoryId": "backend-apis",
      "question": "How would you implement rate limiting in an API?",
      "answer": "Rate limiting controls how many requests a client can make in a time window. Common algorithms: fixed window (simple but allows bursts at window boundaries), sliding window (smoother, tracks exact timestamps), token bucket (allows controlled bursts up to bucket size, refills at fixed rate), and leaky bucket (processes at constant rate, queues excess). Store counters in Redis using INCR with TTL for distributed systems. Return 429 Too Many Requests with Retry-After header. Identify clients by API key, IP, or user ID. Typical limits: 100 requests/minute for authenticated users, 20/minute for anonymous. Use different limits per endpoint — login should be much more restrictive.",
      "difficulty": "hard",
      "tags": [
        "rate-limiting",
        "api-design",
        "redis"
      ]
    },
    {
      "id": "be-010",
      "categoryId": "backend-apis",
      "question": "What are webhooks and how do you design a reliable webhook system?",
      "answer": "Webhooks are HTTP callbacks — your server POSTs to a customer's URL when an event occurs, inverting the polling pattern. For reliability: sign payloads with HMAC-SHA256 so receivers can verify authenticity. Implement exponential backoff retry (e.g., 1min, 5min, 30min, 2h, 24h) for failed deliveries. Store every delivery attempt with status for debugging. Set a 5-10 second timeout per delivery to avoid blocking your queue. Use an async job queue (not inline processing) to send webhooks. Let consumers register URLs and select event types. Include an event ID for idempotent processing on the receiver side. Stripe's webhook design is the gold standard reference.",
      "difficulty": "hard",
      "tags": [
        "webhooks",
        "event-driven",
        "api-design"
      ]
    },
    {
      "id": "be-011",
      "categoryId": "backend-apis",
      "question": "When would you use WebSockets vs long polling vs Server-Sent Events?",
      "answer": "WebSockets provide full-duplex bidirectional communication over a persistent TCP connection — use for chat, multiplayer games, or collaborative editing where both client and server send frequently. Server-Sent Events (SSE) are one-directional from server to client over HTTP, auto-reconnect built in — ideal for live feeds, notifications, and streaming AI responses. Long polling is a fallback where the client makes a request, the server holds it until data is available, then the client immediately re-requests — works everywhere but wastes connections. Use WebSockets for bidirectional real-time, SSE for server-push simplicity, and avoid long polling unless compatibility forces it.",
      "difficulty": "medium",
      "tags": [
        "websockets",
        "sse",
        "real-time",
        "polling"
      ]
    },
    {
      "id": "be-012",
      "categoryId": "backend-apis",
      "question": "How do you apply SOLID principles in a backend application?",
      "answer": "Single Responsibility: each service class handles one domain (UserService, PaymentService). Open/Closed: extend behavior via strategy patterns or middleware rather than modifying existing code. Liskov Substitution: if PaymentProcessor is an interface, StripeProcessor and PayPalProcessor must be interchangeable without breaking callers. Interface Segregation: don't force a service to implement methods it doesn't need — split IReadRepository from IWriteRepository. Dependency Inversion: controllers depend on abstractions (interfaces), not concrete implementations — inject UserRepository rather than PostgresUserRepository. This makes testing trivial because you can inject mocks.",
      "difficulty": "medium",
      "tags": [
        "solid",
        "architecture",
        "design-patterns"
      ]
    },
    {
      "id": "be-013",
      "categoryId": "backend-apis",
      "question": "What is dependency injection and why does it matter?",
      "answer": "Dependency injection means a component receives its dependencies from the outside rather than creating them internally. Instead of a controller instantiating new UserRepository(), the repository is passed in via constructor, parameter, or a DI container. This enables testability (inject a mock database in tests), flexibility (swap implementations without changing consumers), and separation of concerns. In practice, frameworks like NestJS, Spring, and ASP.NET have built-in DI containers that manage object lifetimes (singleton, scoped per request, transient). The core benefit is that high-level business logic doesn't depend on low-level infrastructure details.",
      "difficulty": "easy",
      "tags": [
        "dependency-injection",
        "testing",
        "architecture"
      ]
    },
    {
      "id": "be-014",
      "categoryId": "backend-apis",
      "question": "Compare caching strategies: cache-aside, write-through, write-behind, and read-through.",
      "answer": "Cache-aside (lazy loading): app checks cache first, on miss reads from DB and populates cache — most common, simple, but first request is always slow. Read-through: the cache itself loads from DB on miss — cleaner code but cache must know about the data source. Write-through: writes go to cache and DB synchronously — ensures consistency but adds write latency. Write-behind (write-back): writes go to cache immediately, asynchronously flushed to DB — fast writes but risks data loss on cache failure. For most web apps, cache-aside with Redis and a TTL of 5-60 minutes is the pragmatic default. Invalidate on writes using cache-aside + explicit deletion.",
      "difficulty": "hard",
      "tags": [
        "caching",
        "redis",
        "performance",
        "architecture"
      ]
    },
    {
      "id": "be-015",
      "categoryId": "backend-apis",
      "question": "What does idempotency mean in the context of APIs and why is it critical?",
      "answer": "An idempotent operation produces the same result regardless of how many times it's called. GET, PUT, and DELETE are naturally idempotent — deleting a resource twice should return the same outcome (resource gone). POST is not idempotent by default. This matters because networks are unreliable — if a payment request times out, the client must safely retry without double-charging. Implement it with an idempotency key: the client sends a unique key (UUID) with the request, the server stores the result keyed by that ID, and replays the stored response on retries. Stripe uses this pattern with the Idempotency-Key header.",
      "difficulty": "hard",
      "tags": [
        "idempotency",
        "api-design",
        "reliability"
      ]
    },
    {
      "id": "be-016",
      "categoryId": "backend-apis",
      "question": "Explain database transactions and isolation levels.",
      "answer": "A transaction groups multiple operations into an atomic unit — either all succeed or all roll back. Isolation levels control how concurrent transactions interact. Read Uncommitted allows dirty reads (seeing uncommitted data). Read Committed (Postgres default) only sees committed data but allows non-repeatable reads. Repeatable Read prevents non-repeatable reads but may allow phantom rows. Serializable is the strictest — transactions behave as if executed sequentially, but has the highest performance cost. Most applications use Read Committed with explicit locking (SELECT FOR UPDATE) where stronger guarantees are needed, avoiding the overhead of serializable for all queries.",
      "difficulty": "hard",
      "tags": [
        "transactions",
        "isolation-levels",
        "database",
        "acid"
      ]
    },
    {
      "id": "be-017",
      "categoryId": "backend-apis",
      "question": "What is the N+1 query problem and how do you solve it?",
      "answer": "The N+1 problem occurs when you query a list of N items, then execute one additional query per item to fetch related data — resulting in N+1 total queries. For example, fetching 100 blog posts then querying the author for each one individually. Solutions: use eager loading (JOIN or subquery to fetch all related data in one query), use a DataLoader pattern that batches individual loads into a single WHERE id IN (...) query per tick, or use ORM-specific includes (Prisma's include, Django's select_related, SQLAlchemy's joinedload). Going from 101 queries to 2 queries can reduce response times from seconds to milliseconds.",
      "difficulty": "medium",
      "tags": [
        "n-plus-one",
        "orm",
        "query-optimization",
        "database"
      ]
    },
    {
      "id": "be-018",
      "categoryId": "backend-apis",
      "question": "How do you design an API for handling file uploads?",
      "answer": "For small files (< 10MB), multipart/form-data POST directly to your API is fine — parse with multer (Node) or similar. For large files, use presigned URLs: the client requests an upload URL from your API, then uploads directly to S3/GCS, bypassing your server entirely, and notifies your API when done. This keeps your server stateless and avoids memory issues. Set file size limits, validate MIME types server-side (not just extension), scan for malware if handling untrusted files, and generate unique keys to prevent collisions. Return a file ID or URL in the response, not the file itself.",
      "difficulty": "medium",
      "tags": [
        "file-upload",
        "s3",
        "api-design"
      ]
    },
    {
      "id": "be-019",
      "categoryId": "backend-apis",
      "question": "What is a circuit breaker pattern and when would you use it?",
      "answer": "A circuit breaker prevents cascading failures in distributed systems by wrapping calls to external services. It has three states: Closed (requests flow normally, failures are counted), Open (after a failure threshold is hit, all requests fail immediately without calling the service for a cooldown period), and Half-Open (after cooldown, a few test requests are allowed through to check recovery). This prevents a failing downstream service from overwhelming your system with timeouts and retries. Libraries like opossum (Node.js) or resilience4j (Java) implement this. Use it for any external HTTP call, database connection, or third-party API.",
      "difficulty": "hard",
      "tags": [
        "circuit-breaker",
        "resilience",
        "distributed-systems"
      ]
    },
    {
      "id": "be-020",
      "categoryId": "backend-apis",
      "question": "How would you design a background job processing system?",
      "answer": "Use a message queue (Redis with BullMQ, RabbitMQ, or SQS) where your API enqueues jobs and separate worker processes consume them. Each job should be idempotent (safe to retry), carry all necessary data in its payload (not references that might change), and have a defined retry strategy with exponential backoff and a dead letter queue for permanently failed jobs. Workers should process jobs with timeouts and heartbeats to detect stuck jobs. Track job status (queued, processing, completed, failed) in the database for user visibility. Scale workers independently from your API servers based on queue depth.",
      "difficulty": "hard",
      "tags": [
        "job-queue",
        "background-processing",
        "architecture"
      ]
    },
    {
      "id": "db-001",
      "categoryId": "databases-sql",
      "question": "Explain the different types of SQL JOINs with examples of when to use each.",
      "answer": "INNER JOIN returns only rows with matches in both tables — use for required relationships (orders with their products). LEFT JOIN returns all rows from the left table plus matches from the right (nulls where no match) — use when the right side is optional (users with their optional profile). RIGHT JOIN is the inverse, rarely used since you can just swap table order with LEFT JOIN. FULL OUTER JOIN returns all rows from both tables — useful for reconciliation queries (find records in either system). CROSS JOIN produces the Cartesian product (every row paired with every other row) — use for generating combinations like all date-product pairs for a report.",
      "difficulty": "easy",
      "tags": [
        "sql",
        "joins",
        "queries"
      ]
    },
    {
      "id": "db-002",
      "categoryId": "databases-sql",
      "question": "How do database indexes work and what is the difference between B-tree and hash indexes?",
      "answer": "An index is a separate data structure that maps column values to row locations, enabling the database to find rows without scanning the entire table. B-tree indexes (the default) store data in a sorted, balanced tree structure, supporting equality (=), range queries (<, >, BETWEEN), ORDER BY, and prefix LIKE ('abc%'). Hash indexes compute a hash of the value for O(1) lookups but only support exact equality — no ranges or sorting. B-tree is the right choice 95% of the time. A table scan on 1M rows might take 500ms; with a B-tree index, the same lookup takes under 1ms by traversing ~3-4 tree levels.",
      "difficulty": "medium",
      "tags": [
        "indexes",
        "b-tree",
        "query-optimization"
      ]
    },
    {
      "id": "db-003",
      "categoryId": "databases-sql",
      "question": "What is database normalization? Explain 1NF through 3NF.",
      "answer": "Normalization reduces data redundancy and update anomalies by organizing tables. First Normal Form (1NF): every column holds atomic values (no arrays or comma-separated lists), each row is unique. Second Normal Form (2NF): 1NF plus every non-key column depends on the entire primary key (eliminates partial dependencies in composite keys). Third Normal Form (3NF): 2NF plus no non-key column depends on another non-key column (eliminates transitive dependencies). Example: a table with order_id, product_id, product_name violates 3NF because product_name depends on product_id, not the primary key — split products into their own table. In practice, most OLTP systems target 3NF, while analytics schemas intentionally denormalize for query speed.",
      "difficulty": "medium",
      "tags": [
        "normalization",
        "database-design",
        "schema"
      ]
    },
    {
      "id": "db-004",
      "categoryId": "databases-sql",
      "question": "What are the ACID properties and why do they matter?",
      "answer": "ACID guarantees reliable database transactions. Atomicity: a transaction is all-or-nothing — if any part fails, everything rolls back. Consistency: a transaction moves the database from one valid state to another, enforcing all constraints. Isolation: concurrent transactions don't interfere with each other (the degree is tunable via isolation levels). Durability: once committed, data survives crashes (written to disk/WAL before acknowledging). These matter because without them, you get corrupted data — imagine a bank transfer that deducts from one account but crashes before crediting the other. NoSQL databases often sacrifice some ACID properties (especially isolation) for availability and partition tolerance.",
      "difficulty": "easy",
      "tags": [
        "acid",
        "transactions",
        "database-fundamentals"
      ]
    },
    {
      "id": "db-005",
      "categoryId": "databases-sql",
      "question": "How do database transactions work in practice? When would you use savepoints?",
      "answer": "You begin a transaction with BEGIN, execute your statements, then COMMIT if everything succeeds or ROLLBACK if anything fails. The database uses a write-ahead log (WAL) to track changes and guarantee atomicity. Savepoints create checkpoints within a transaction — you can ROLLBACK TO SAVEPOINT without aborting the entire transaction. Use savepoints when a batch operation should continue even if one item fails, or in nested service calls where an inner operation might fail but the outer transaction should survive. In application code, use try/catch around each savepoint block to handle partial failures gracefully.",
      "difficulty": "medium",
      "tags": [
        "transactions",
        "savepoints",
        "sql"
      ]
    },
    {
      "id": "db-006",
      "categoryId": "databases-sql",
      "question": "What are stored procedures and when should (or shouldn't) you use them?",
      "answer": "Stored procedures are precompiled SQL programs stored in the database, executed with CALL or EXEC. Advantages: they reduce network round trips by running complex logic server-side, enforce data rules regardless of which application connects, and the execution plan is cached. Disadvantages: they're harder to version control, test, and debug than application code; they couple business logic to the database vendor; and they can become a maintenance nightmare when hundreds accumulate. Use them for performance-critical batch operations, data integrity rules that must be enforced regardless of client, and complex reports. Avoid them as a replacement for application business logic.",
      "difficulty": "medium",
      "tags": [
        "stored-procedures",
        "sql",
        "database-design"
      ]
    },
    {
      "id": "db-007",
      "categoryId": "databases-sql",
      "question": "How do you use EXPLAIN to optimize a slow query?",
      "answer": "Run EXPLAIN ANALYZE before your query (in Postgres) to see the actual execution plan with real timing. Key things to look for: Seq Scan on large tables means a missing index, Nested Loop with high row counts means potential N+1, Sort with high memory means a missing index on the ORDER BY column, and large differences between estimated and actual rows mean stale statistics (run ANALYZE). The cost numbers are relative — compare before and after adding an index. Look for the widest/most expensive node and optimize that first. Common fixes: add an index, rewrite subqueries as JOINs, add WHERE clauses to filter earlier, or use covering indexes to avoid table lookups.",
      "difficulty": "hard",
      "tags": [
        "explain",
        "query-optimization",
        "performance",
        "postgresql"
      ]
    },
    {
      "id": "db-008",
      "categoryId": "databases-sql",
      "question": "When would you choose a NoSQL database over a relational database?",
      "answer": "Choose NoSQL when your data model doesn't fit tables well: document stores (MongoDB) for heterogeneous nested objects like product catalogs with varying attributes, key-value stores (Redis) for caching and session data, wide-column stores (Cassandra) for massive write throughput across distributed nodes, and graph databases (Neo4j) for relationship-heavy queries like social networks. Choose relational when you need ACID transactions, complex joins, strong consistency, and your data has clear relationships. Most applications should start with PostgreSQL — it handles JSON, full-text search, and scales further than people think. Only adopt NoSQL when you have a specific problem SQL can't solve efficiently.",
      "difficulty": "medium",
      "tags": [
        "nosql",
        "sql",
        "database-selection",
        "architecture"
      ]
    },
    {
      "id": "db-009",
      "categoryId": "databases-sql",
      "question": "How does MongoDB store and query data? What are its strengths and weaknesses?",
      "answer": "MongoDB stores data as BSON documents (binary JSON) in collections. Documents can have nested objects and arrays, and schema is flexible (different documents in the same collection can have different fields). Queries use a JSON-like syntax: db.users.find({ age: { $gt: 25 }, 'address.city': 'Chicago' }). Strengths: flexible schema speeds up development, horizontal scaling via sharding is built-in, and nested document reads are fast (no joins). Weaknesses: no multi-document ACID transactions (until v4.0, and they're still expensive), data duplication instead of normalization leads to update anomalies, and complex aggregation pipelines are harder to write than SQL joins.",
      "difficulty": "medium",
      "tags": [
        "mongodb",
        "nosql",
        "document-database"
      ]
    },
    {
      "id": "db-010",
      "categoryId": "databases-sql",
      "question": "What are the main Redis data structures and when would you use each?",
      "answer": "Strings: simple key-value, used for caching, counters (INCR), and session tokens. Lists: ordered collections, used for message queues (LPUSH/RPOP) and activity feeds. Sets: unique unordered collections, used for tags, unique visitors, and set operations (SINTER for common friends). Sorted Sets: sets with a score, used for leaderboards (ZADD/ZRANGE), rate limiters (sliding window), and priority queues. Hashes: field-value maps under one key, used for objects (user:123 with name, email fields) — more memory-efficient than separate string keys. Streams: append-only log for event sourcing and message processing. Redis processes everything in-memory and single-threaded, giving sub-millisecond latency.",
      "difficulty": "medium",
      "tags": [
        "redis",
        "data-structures",
        "caching"
      ]
    },
    {
      "id": "db-011",
      "categoryId": "databases-sql",
      "question": "How does database sharding work and what are the tradeoffs?",
      "answer": "Sharding horizontally partitions data across multiple database servers. A shard key (e.g., user_id) determines which shard holds each row. Hash-based sharding (hash(user_id) % N) distributes evenly but makes range queries hard. Range-based sharding (users 1-1M on shard 1, 1M-2M on shard 2) supports range queries but can create hotspots. Tradeoffs: you gain linear write scalability but lose cross-shard joins (must query multiple shards and merge in application), cross-shard transactions become extremely complex, and resharding when you add nodes requires data migration. Avoid sharding as long as possible — vertical scaling, read replicas, and partitioning solve most problems up to billions of rows.",
      "difficulty": "hard",
      "tags": [
        "sharding",
        "distributed-systems",
        "scalability"
      ]
    },
    {
      "id": "db-012",
      "categoryId": "databases-sql",
      "question": "Explain master-slave replication and when you'd use it.",
      "answer": "In master-slave (primary-replica) replication, one master handles all writes and streams changes (via WAL or binlog) to one or more read-only replicas. Replicas serve read queries, distributing load. This scales reads linearly — add more replicas for more read throughput. The tradeoff is replication lag: a write to the master may take milliseconds to appear on replicas, causing stale reads. Handle this with read-your-own-writes consistency (route the writing user's subsequent reads to master for a short window). Use it when your workload is read-heavy (most web apps are 90%+ reads). For write scaling, you need sharding instead.",
      "difficulty": "medium",
      "tags": [
        "replication",
        "master-slave",
        "scalability",
        "database"
      ]
    },
    {
      "id": "db-013",
      "categoryId": "databases-sql",
      "question": "How does connection pooling work at the database level and why does pool size matter?",
      "answer": "A connection pool pre-opens a fixed number of TCP connections to the database. When your app needs to query, it borrows a connection from the pool, uses it, and returns it. This avoids the 20-50ms cost of establishing a new connection per query. Pool size is critical: too small and requests queue up waiting for a connection, too large and you overwhelm the database with concurrent queries fighting for CPU and I/O. PostgreSQL recommends connections = (CPU cores * 2) + number_of_disks, typically 10-20 per application instance. For many app servers, use PgBouncer as an external pooler to multiplex hundreds of app connections onto a smaller number of database connections.",
      "difficulty": "medium",
      "tags": [
        "connection-pooling",
        "performance",
        "postgresql"
      ]
    },
    {
      "id": "db-014",
      "categoryId": "databases-sql",
      "question": "What causes deadlocks in a database and how do you prevent them?",
      "answer": "A deadlock occurs when two transactions each hold a lock the other needs, creating a circular wait. For example: Transaction A locks row 1 then tries to lock row 2, while Transaction B locks row 2 then tries to lock row 1. The database detects this (via a wait-for graph) and kills one transaction, rolling it back. Prevention strategies: always acquire locks in a consistent order (e.g., by ascending ID), keep transactions short to minimize lock hold time, use SELECT FOR UPDATE SKIP LOCKED for queue-like patterns, and avoid upgrading from shared to exclusive locks within a transaction. In application code, implement retry logic for deadlock errors (SQLSTATE 40P01 in Postgres).",
      "difficulty": "hard",
      "tags": [
        "deadlocks",
        "concurrency",
        "transactions",
        "locking"
      ]
    },
    {
      "id": "db-015",
      "categoryId": "databases-sql",
      "question": "Explain SQL window functions with practical examples.",
      "answer": "Window functions perform calculations across a set of rows related to the current row without collapsing them (unlike GROUP BY). The OVER clause defines the window. ROW_NUMBER() OVER(PARTITION BY dept ORDER BY salary DESC) assigns a rank per department — useful for finding the top-N per group. SUM(sales) OVER(ORDER BY date ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) computes a running total. LAG(salary, 1) OVER(ORDER BY hire_date) gives the previous row's salary — great for period-over-period comparisons. NTILE(4) OVER(ORDER BY score) splits rows into quartiles. Window functions are often the cleanest way to solve problems that would otherwise require self-joins or correlated subqueries.",
      "difficulty": "hard",
      "tags": [
        "window-functions",
        "sql",
        "analytics"
      ]
    },
    {
      "id": "db-016",
      "categoryId": "databases-sql",
      "question": "What are Common Table Expressions (CTEs) and when are they useful?",
      "answer": "A CTE (WITH clause) defines a temporary named result set that exists only for the duration of the query. WITH active_users AS (SELECT * FROM users WHERE last_login > NOW() - INTERVAL '30 days') SELECT * FROM active_users JOIN orders ON ... CTEs improve readability by breaking complex queries into logical steps. Recursive CTEs (WITH RECURSIVE) can traverse hierarchical data like org charts or category trees. In PostgreSQL, CTEs are optimization fences by default (materialized) before v12, meaning the optimizer can't push predicates into them — use them for clarity but be aware of performance. They're also essential for recursive queries that self-joins cannot express.",
      "difficulty": "medium",
      "tags": [
        "cte",
        "sql",
        "recursive-queries"
      ]
    },
    {
      "id": "db-017",
      "categoryId": "databases-sql",
      "question": "Explain foreign keys and common database constraints. What happens when you violate them?",
      "answer": "Foreign keys enforce referential integrity — the child table's column must reference an existing value in the parent table. ON DELETE CASCADE deletes child rows when the parent is deleted; ON DELETE SET NULL nullifies the reference; ON DELETE RESTRICT (default) prevents parent deletion. Other constraints: PRIMARY KEY (unique + not null), UNIQUE (no duplicates, nulls allowed), NOT NULL, CHECK (custom boolean condition like CHECK(price > 0)), and DEFAULT. Violating a constraint raises an error and the statement rolls back. Constraints are your last line of defense — they catch bugs that application code misses and guarantee data integrity regardless of which client writes to the database.",
      "difficulty": "easy",
      "tags": [
        "constraints",
        "foreign-keys",
        "database-design"
      ]
    },
    {
      "id": "db-018",
      "categoryId": "databases-sql",
      "question": "What is table partitioning and when should you use it?",
      "answer": "Partitioning splits a large table into smaller physical pieces (partitions) while maintaining a single logical table interface. Range partitioning splits by value ranges (e.g., one partition per month of data), list partitioning by discrete values (e.g., by country), and hash partitioning distributes evenly by hash. Benefits: queries that filter on the partition key scan only relevant partitions (partition pruning), bulk deletes become instant (DROP PARTITION instead of DELETE), and maintenance operations (VACUUM, reindex) work on smaller chunks. Use partitioning for tables with hundreds of millions of rows, especially time-series data. PostgreSQL native partitioning (v10+) handles this declaratively with PARTITION BY.",
      "difficulty": "hard",
      "tags": [
        "partitioning",
        "performance",
        "postgresql",
        "large-tables"
      ]
    },
    {
      "id": "db-019",
      "categoryId": "databases-sql",
      "question": "What are materialized views and how do they differ from regular views?",
      "answer": "A regular view is a saved query — it executes the underlying SQL every time you select from it, with no storage cost. A materialized view stores the query result physically on disk, like a cached table. It's fast to read (just a table scan) but shows stale data until you REFRESH MATERIALIZED VIEW. Use CONCURRENTLY to refresh without locking reads. Materialized views are ideal for expensive analytical queries (aggregations, complex joins) that don't need real-time freshness — dashboard queries, reports, denormalized search tables. Refresh on a schedule (e.g., every 5 minutes via pg_cron) or trigger after relevant data changes. In Postgres, you can create indexes on materialized views for even faster lookups.",
      "difficulty": "hard",
      "tags": [
        "materialized-views",
        "views",
        "performance",
        "postgresql"
      ]
    },
    {
      "id": "db-020",
      "categoryId": "databases-sql",
      "question": "How do you design an index strategy for a table? What are covering indexes and composite indexes?",
      "answer": "A composite index covers multiple columns — CREATE INDEX ON orders(customer_id, created_at). Column order matters: the index is useful for queries filtering on customer_id alone or customer_id + created_at, but not created_at alone (leftmost prefix rule). A covering index includes all columns a query needs, so the database reads entirely from the index without hitting the table (index-only scan). In Postgres: CREATE INDEX ON orders(customer_id) INCLUDE (total, status). Strategy: index columns in WHERE, JOIN, and ORDER BY clauses. Check pg_stat_user_indexes for unused indexes (they slow writes). Don't over-index — each index costs write performance and storage. Use EXPLAIN to verify your indexes are actually being used.",
      "difficulty": "hard",
      "tags": [
        "indexes",
        "composite-index",
        "covering-index",
        "query-optimization"
      ]
    },
    {
      "id": "db-021",
      "categoryId": "databases-sql",
      "question": "What is a self-join and when would you use it?",
      "answer": "A self-join is when a table joins to itself using different aliases. It's used when rows in the same table have a relationship to each other. Classic example: an employees table with a manager_id column — SELECT e.name, m.name AS manager FROM employees e LEFT JOIN employees m ON e.manager_id = m.id. Other use cases: finding duplicate records, comparing rows within the same table (e.g., flights where departure city of one row matches arrival city of another), and finding sequential events (join a log table to itself where timestamps are adjacent). Ensure you have indexes on the join columns to avoid quadratic performance.",
      "difficulty": "easy",
      "tags": [
        "sql",
        "joins",
        "self-join"
      ]
    },
    {
      "id": "db-022",
      "categoryId": "databases-sql",
      "question": "Explain the difference between DELETE, TRUNCATE, and DROP.",
      "answer": "DELETE removes rows one at a time, fires triggers, can have a WHERE clause, is fully logged and rollbackable, and does not reset auto-increment counters. TRUNCATE removes all rows instantly by deallocating data pages — it's much faster on large tables, resets auto-increment, but cannot have a WHERE clause and fires no row-level triggers. DROP removes the entire table structure, data, indexes, and constraints permanently. In Postgres, TRUNCATE is transactional (can be rolled back); in MySQL it's not. Use DELETE for selective removal, TRUNCATE for clearing a table you'll reuse, and DROP to remove the table entirely. TRUNCATE on a 10M-row table takes milliseconds vs minutes for DELETE.",
      "difficulty": "easy",
      "tags": [
        "sql",
        "delete",
        "truncate",
        "ddl"
      ]
    },
    {
      "id": "db-023",
      "categoryId": "databases-sql",
      "question": "How does PostgreSQL's MVCC (Multi-Version Concurrency Control) work?",
      "answer": "MVCC allows readers and writers to operate concurrently without blocking each other. Instead of locking rows for reads, PostgreSQL keeps multiple versions of each row. Each transaction sees a snapshot of the database at its start time. When a row is updated, Postgres creates a new version (tuple) with the new data and marks the old version as expired. Readers see the version valid for their snapshot, while writers create new versions. Dead tuples (old versions no longer visible to any transaction) are cleaned up by VACUUM. This means SELECT never blocks INSERT/UPDATE and vice versa, but it requires regular vacuuming to reclaim space and prevent table bloat.",
      "difficulty": "hard",
      "tags": [
        "mvcc",
        "postgresql",
        "concurrency",
        "internals"
      ]
    },
    {
      "id": "db-024",
      "categoryId": "databases-sql",
      "question": "What is the difference between HAVING and WHERE in SQL?",
      "answer": "WHERE filters individual rows before any grouping happens — it operates on the raw table data. HAVING filters groups after GROUP BY aggregation. For example: SELECT department, AVG(salary) FROM employees WHERE status = 'active' GROUP BY department HAVING AVG(salary) > 80000. The WHERE clause excludes inactive employees before aggregation, then HAVING excludes departments whose average salary is too low. You cannot use aggregate functions in WHERE (WHERE AVG(salary) > 80000 is a syntax error) because aggregates don't exist yet at that stage of query execution. Always filter with WHERE when possible — it reduces rows before aggregation, which is more efficient.",
      "difficulty": "easy",
      "tags": [
        "sql",
        "having",
        "where",
        "aggregation"
      ]
    },
    {
      "id": "db-025",
      "categoryId": "databases-sql",
      "question": "How would you migrate a production database schema with zero downtime?",
      "answer": "Zero-downtime migrations require additive-only changes deployed in phases. To rename a column: add new column, deploy code that writes to both columns, backfill old data, deploy code that reads from new column, then drop old column. To add a NOT NULL column: add it as nullable first, backfill with a default, then add the NOT NULL constraint. In Postgres, adding a column with a DEFAULT is instant since v11 (no table rewrite). Never lock a hot table with ALTER TABLE that rewrites all rows. Use tools like pg_repack for table restructuring without locks. Run migrations in transactions with a lock timeout (SET lock_timeout = '5s') so they fail fast rather than blocking all queries. Tools like gh-ost (MySQL) or pgroll (Postgres) automate safe online migrations.",
      "difficulty": "hard",
      "tags": [
        "migrations",
        "zero-downtime",
        "postgresql",
        "devops"
      ]
    },
    {
      "id": "devops-001",
      "categoryId": "devops-cloud",
      "question": "What is the difference between a Docker image and a container?",
      "answer": "An image is a read-only template built from a Dockerfile containing your app code, runtime, libraries, and config. A container is a running instance of an image with its own writable layer on top. You can spin up many containers from the same image, each isolated with its own filesystem, network, and process space.",
      "difficulty": "easy",
      "tags": [
        "docker",
        "containers",
        "images"
      ]
    },
    {
      "id": "devops-002",
      "categoryId": "devops-cloud",
      "question": "How does Docker Compose differ from plain Docker run commands?",
      "answer": "Docker Compose lets you define multi-container applications in a single YAML file (docker-compose.yml), specifying services, networks, volumes, and environment variables declaratively. Instead of running multiple docker run commands with complex flags, you run docker compose up and the entire stack starts with proper networking between services. It also handles dependency ordering with depends_on and simplifies local development workflows.",
      "difficulty": "easy",
      "tags": [
        "docker",
        "docker-compose",
        "orchestration"
      ]
    },
    {
      "id": "devops-003",
      "categoryId": "devops-cloud",
      "question": "Explain the role of a Kubernetes Pod and why it is the smallest deployable unit, not a container.",
      "answer": "A Pod wraps one or more containers that share the same network namespace (same IP, localhost communication) and can share volumes. Kubernetes schedules Pods, not individual containers, because co-located containers often need tight coupling like a sidecar logging agent next to an app server. Pods are ephemeral by design; controllers like Deployments manage their lifecycle and replacement.",
      "difficulty": "medium",
      "tags": [
        "kubernetes",
        "pods",
        "scheduling"
      ]
    },
    {
      "id": "devops-004",
      "categoryId": "devops-cloud",
      "question": "What is a Kubernetes Deployment and how does it handle rolling updates?",
      "answer": "A Deployment manages a ReplicaSet that ensures a desired number of identical Pods are running. During a rolling update, it creates a new ReplicaSet with updated Pods while scaling down the old one incrementally. You control the rollout speed with maxSurge (extra Pods allowed) and maxUnavailable (Pods that can be down). If something breaks, kubectl rollout undo reverts to the previous ReplicaSet instantly.",
      "difficulty": "medium",
      "tags": [
        "kubernetes",
        "deployments",
        "rolling-updates"
      ]
    },
    {
      "id": "devops-005",
      "categoryId": "devops-cloud",
      "question": "Walk me through a typical CI/CD pipeline from commit to production.",
      "answer": "A developer pushes code, triggering the CI pipeline: lint, unit tests, build artifact (Docker image or binary), integration tests, then publish the artifact to a registry. The CD pipeline picks up the new artifact, deploys to a staging environment, runs smoke tests or acceptance tests, then promotes to production using a strategy like blue-green or canary. Each stage acts as a gate; failure at any point stops the pipeline and notifies the team.",
      "difficulty": "medium",
      "tags": [
        "ci-cd",
        "pipelines",
        "automation"
      ]
    },
    {
      "id": "devops-006",
      "categoryId": "devops-cloud",
      "question": "What is Terraform state and why is remote state important?",
      "answer": "Terraform state is a JSON file that maps your declared resources to real infrastructure IDs so Terraform knows what exists and what to change. Without it, Terraform would try to recreate everything on every run. Remote state (stored in S3, GCS, or Terraform Cloud) enables team collaboration, state locking to prevent concurrent modifications, and versioning for rollback. Losing state means you must import every resource manually or risk destructive re-creation.",
      "difficulty": "medium",
      "tags": [
        "terraform",
        "infrastructure-as-code",
        "state-management"
      ]
    },
    {
      "id": "devops-007",
      "categoryId": "devops-cloud",
      "question": "How does Prometheus collect and store metrics, and how does it differ from push-based monitoring?",
      "answer": "Prometheus uses a pull model: it scrapes HTTP endpoints (/metrics) on targets at configurable intervals, typically 15-30 seconds. Metrics are stored in a local time-series database with label-based indexing. This differs from push-based systems like StatsD or Datadog Agent where applications send metrics to a collector. The pull model makes it easy to detect when a target is down (failed scrape) and avoids overwhelming the monitoring system during traffic spikes.",
      "difficulty": "medium",
      "tags": [
        "prometheus",
        "monitoring",
        "metrics"
      ]
    },
    {
      "id": "devops-008",
      "categoryId": "devops-cloud",
      "question": "What are the three components of the ELK stack and what role does each play?",
      "answer": "Elasticsearch is the distributed search and analytics engine that indexes and stores log data. Logstash is the pipeline that ingests logs from multiple sources, transforms them (parsing, filtering, enriching), and ships them to Elasticsearch. Kibana is the visualization layer providing dashboards, search, and alerting on top of Elasticsearch. In modern setups, lightweight Filebeat or Fluentd often replaces Logstash for log shipping, with Logstash reserved for heavy transformations.",
      "difficulty": "easy",
      "tags": [
        "elk",
        "logging",
        "elasticsearch",
        "kibana"
      ]
    },
    {
      "id": "devops-009",
      "categoryId": "devops-cloud",
      "question": "Explain the difference between an Application Load Balancer and a Network Load Balancer on AWS.",
      "answer": "An ALB operates at Layer 7 (HTTP/HTTPS), can route based on URL paths, hostnames, headers, and query strings, and supports features like sticky sessions and WebSocket upgrades. An NLB operates at Layer 4 (TCP/UDP), handles millions of requests per second with ultra-low latency, preserves the client source IP, and supports static IPs or Elastic IPs. Use ALB for web apps needing content-based routing; use NLB for extreme throughput, non-HTTP protocols, or when you need a fixed IP.",
      "difficulty": "medium",
      "tags": [
        "aws",
        "load-balancers",
        "alb",
        "nlb"
      ]
    },
    {
      "id": "devops-010",
      "categoryId": "devops-cloud",
      "question": "How does AWS Auto Scaling work and what metrics would you scale on?",
      "answer": "Auto Scaling adjusts the number of EC2 instances (or ECS tasks, etc.) based on scaling policies tied to CloudWatch metrics. Common metrics include CPU utilization (target ~70%), request count per target on an ALB, custom metrics like queue depth, or scheduled scaling for predictable traffic patterns. It uses a cooldown period to avoid thrashing. You define min, max, and desired capacity, and the scaling policy determines how aggressively to add or remove instances.",
      "difficulty": "medium",
      "tags": [
        "aws",
        "auto-scaling",
        "ec2",
        "cloudwatch"
      ]
    },
    {
      "id": "devops-011",
      "categoryId": "devops-cloud",
      "question": "What is a blue-green deployment and when would you choose it over canary?",
      "answer": "Blue-green maintains two identical production environments. Traffic goes entirely to blue (current) while green gets the new version. After testing green, you switch the load balancer or DNS to point all traffic to green instantly. Choose blue-green when you need instant rollback (just switch back to blue) and can afford running double infrastructure. Canary is better when you want to gradually validate with a small percentage of real users (e.g., 5% then 25% then 100%) to catch issues that only appear at scale.",
      "difficulty": "hard",
      "tags": [
        "deployments",
        "blue-green",
        "canary",
        "release-strategies"
      ]
    },
    {
      "id": "devops-012",
      "categoryId": "devops-cloud",
      "question": "How would you manage secrets in a production Kubernetes cluster?",
      "answer": "Use an external secrets manager like AWS Secrets Manager, HashiCorp Vault, or GCP Secret Manager, and sync secrets into Kubernetes using the External Secrets Operator or CSI Secrets Store driver. Avoid baking secrets into images or storing them in plain ConfigMaps. Kubernetes native Secrets are base64-encoded (not encrypted) by default, so enable encryption at rest with a KMS provider. Rotate secrets automatically and use RBAC to restrict which Pods and service accounts can access which secrets.",
      "difficulty": "hard",
      "tags": [
        "kubernetes",
        "secrets-management",
        "security",
        "vault"
      ]
    },
    {
      "id": "devops-013",
      "categoryId": "devops-cloud",
      "question": "Explain VPC, subnets, and why you separate public and private subnets.",
      "answer": "A VPC is your isolated virtual network in the cloud with its own CIDR block (e.g., 10.0.0.0/16). Subnets partition that range across availability zones. Public subnets have a route to an Internet Gateway so resources get public IPs; private subnets route outbound traffic through a NAT Gateway but are unreachable from the internet. You put load balancers and bastion hosts in public subnets, and application servers, databases, and caches in private subnets to minimize attack surface.",
      "difficulty": "medium",
      "tags": [
        "aws",
        "vpc",
        "networking",
        "subnets"
      ]
    },
    {
      "id": "devops-014",
      "categoryId": "devops-cloud",
      "question": "How does DNS resolution work from a browser to your application?",
      "answer": "The browser checks its local cache, then the OS resolver cache, then queries the configured recursive DNS resolver (e.g., ISP or 8.8.8.8). The recursive resolver walks the DNS hierarchy: root servers point to TLD servers (.com), which point to your domain's authoritative nameservers (e.g., Route 53). The authoritative server returns the IP (A/AAAA record) or CNAME. Each response has a TTL controlling how long it is cached. The entire process typically takes 20-120ms on a cold lookup.",
      "difficulty": "easy",
      "tags": [
        "dns",
        "networking",
        "resolution"
      ]
    },
    {
      "id": "devops-015",
      "categoryId": "devops-cloud",
      "question": "What is the TLS handshake and what happens during it?",
      "answer": "In TLS 1.3, the client sends a ClientHello with supported cipher suites and a key share. The server responds with its chosen cipher suite, its certificate, and its key share. The client verifies the certificate chain against trusted CAs, both sides derive session keys from the key exchange, and encrypted communication begins. TLS 1.3 completes in one round trip (1-RTT) compared to two in TLS 1.2. The handshake ensures authentication (certificate), confidentiality (encryption), and integrity (MAC).",
      "difficulty": "hard",
      "tags": [
        "tls",
        "ssl",
        "security",
        "networking"
      ]
    },
    {
      "id": "devops-016",
      "categoryId": "devops-cloud",
      "question": "What is the difference between EC2, Lambda, and ECS on AWS?",
      "answer": "EC2 gives you full virtual machines you manage (OS, patching, scaling). Lambda is serverless: you upload code, it runs on demand per invocation with zero server management, billing per 1ms of execution, but has a 15-minute timeout and cold start latency. ECS is a container orchestrator: you define tasks (Docker containers), and ECS schedules them on EC2 instances or Fargate (serverless containers). Choose Lambda for event-driven, short-lived workloads; ECS for long-running containerized services; EC2 for full control or legacy apps.",
      "difficulty": "easy",
      "tags": [
        "aws",
        "ec2",
        "lambda",
        "ecs"
      ]
    },
    {
      "id": "devops-017",
      "categoryId": "devops-cloud",
      "question": "How does S3 achieve eleven nines of durability?",
      "answer": "S3 automatically replicates objects across a minimum of three Availability Zones within a region, each in a physically separate data center. It uses checksums to detect bit-rot and automatically heals corrupted copies from healthy replicas. The 99.999999999% durability means statistically you would lose one object out of 10 billion every 10,000 years. S3 also supports versioning to protect against accidental deletes and Cross-Region Replication for disaster recovery.",
      "difficulty": "hard",
      "tags": [
        "aws",
        "s3",
        "durability",
        "storage"
      ]
    },
    {
      "id": "devops-018",
      "categoryId": "devops-cloud",
      "question": "What is a multi-stage Docker build and why use it?",
      "answer": "A multi-stage build uses multiple FROM statements in a single Dockerfile. Earlier stages install build tools, compile code, and run tests. The final stage copies only the compiled artifact into a minimal base image like alpine or distroless. This produces dramatically smaller images (e.g., 900MB build stage down to a 50MB production image) with fewer vulnerabilities since build tools, source code, and dev dependencies are not shipped to production.",
      "difficulty": "easy",
      "tags": [
        "docker",
        "dockerfile",
        "optimization"
      ]
    },
    {
      "id": "devops-019",
      "categoryId": "devops-cloud",
      "question": "Explain Terraform plan vs apply and what happens with drift.",
      "answer": "Terraform plan reads your .tf files and current state, queries the cloud provider for actual resource status, and shows a diff of what will be created, changed, or destroyed. Apply executes that plan. Drift occurs when someone changes infrastructure outside Terraform (console click, CLI). On the next plan, Terraform detects the drift and proposes changes to bring reality back in line with your code. You can also run terraform refresh to update state without applying, though this is implicit in plan since v0.15.4.",
      "difficulty": "medium",
      "tags": [
        "terraform",
        "infrastructure-as-code",
        "drift"
      ]
    },
    {
      "id": "devops-020",
      "categoryId": "devops-cloud",
      "question": "How would you design a highly available RDS deployment?",
      "answer": "Use Multi-AZ deployment where AWS maintains a synchronous standby replica in a different Availability Zone. Failover is automatic (typically 60-120 seconds) with DNS endpoint unchanged. Add read replicas (up to 15 for Aurora) to offload read traffic. Enable automated backups with point-in-time recovery, set appropriate backup retention (e.g., 7-35 days), and use encryption at rest with KMS. Put the RDS instance in private subnets, restrict access via security groups, and use IAM authentication where supported.",
      "difficulty": "hard",
      "tags": [
        "aws",
        "rds",
        "high-availability",
        "databases"
      ]
    },
    {
      "id": "oop-001",
      "categoryId": "oop-design-patterns",
      "question": "Explain the Single Responsibility Principle with a real example.",
      "answer": "SRP states that a class should have only one reason to change, meaning it should do one thing well. For example, an Invoice class should calculate totals but should not also send emails and generate PDFs. If email requirements change, you should not have to touch invoice calculation code. Split it into Invoice, InvoiceMailer, and InvoicePrinter. This reduces coupling, makes testing easier, and limits the blast radius of changes.",
      "difficulty": "easy",
      "tags": [
        "solid",
        "srp",
        "design-principles"
      ]
    },
    {
      "id": "oop-002",
      "categoryId": "oop-design-patterns",
      "question": "What is the Open/Closed Principle and how do you achieve it?",
      "answer": "OCP states that classes should be open for extension but closed for modification. You should be able to add new behavior without changing existing code. Achieve it through abstractions: define an interface or abstract class, then add new implementations. For example, a PaymentProcessor interface with CreditCardProcessor and PayPalProcessor. When you add CryptoProcessor, you create a new class implementing the interface without touching existing processors or the code that uses them.",
      "difficulty": "medium",
      "tags": [
        "solid",
        "ocp",
        "design-principles"
      ]
    },
    {
      "id": "oop-003",
      "categoryId": "oop-design-patterns",
      "question": "Explain the Liskov Substitution Principle. What is a classic violation?",
      "answer": "LSP says subclasses must be substitutable for their base class without breaking correctness. The classic violation is Rectangle/Square: if Square extends Rectangle and overrides setWidth to also set height, code that sets width and height independently on a Rectangle will break when handed a Square. The fix is to not use inheritance here; both can implement a Shape interface instead. LSP violations often manifest as instanceof checks scattered through the codebase.",
      "difficulty": "hard",
      "tags": [
        "solid",
        "lsp",
        "inheritance"
      ]
    },
    {
      "id": "oop-004",
      "categoryId": "oop-design-patterns",
      "question": "What is the Interface Segregation Principle?",
      "answer": "ISP states that clients should not be forced to depend on interfaces they do not use. Instead of one fat interface with 20 methods, split it into smaller, focused interfaces. For example, a Worker interface with work() and eat() forces Robot to implement eat(). Split into Workable and Feedable. Classes implement only what they need. This prevents dummy implementations, reduces recompilation when interfaces change, and makes the system easier to understand.",
      "difficulty": "medium",
      "tags": [
        "solid",
        "isp",
        "interfaces"
      ]
    },
    {
      "id": "oop-005",
      "categoryId": "oop-design-patterns",
      "question": "Explain Dependency Inversion Principle and how it differs from dependency injection.",
      "answer": "DIP says high-level modules should not depend on low-level modules; both should depend on abstractions. For example, an OrderService should depend on a PaymentGateway interface, not directly on StripeClient. Dependency injection is a technique for achieving DIP: instead of a class creating its own dependencies (new StripeClient()), they are injected via constructor, setter, or framework. DIP is the principle; DI is the mechanism.",
      "difficulty": "medium",
      "tags": [
        "solid",
        "dip",
        "dependency-injection"
      ]
    },
    {
      "id": "oop-006",
      "categoryId": "oop-design-patterns",
      "question": "When would you prefer composition over inheritance?",
      "answer": "Prefer composition when you need flexibility to change behavior at runtime, when the relationship is 'has-a' rather than 'is-a', or when you want to combine behaviors from multiple sources (since most languages allow single inheritance only). For example, instead of Bird extends FlyingAnimal, use Bird has a FlyBehavior. Composition avoids fragile base class problems, deep inheritance hierarchies, and makes testing easier since you can inject mock components. Use inheritance mainly for true polymorphic type hierarchies with stable interfaces.",
      "difficulty": "medium",
      "tags": [
        "composition",
        "inheritance",
        "design-principles"
      ]
    },
    {
      "id": "oop-007",
      "categoryId": "oop-design-patterns",
      "question": "What is the difference between an abstract class and an interface?",
      "answer": "An abstract class can have implemented methods, instance state (fields), and constructors, and a class can extend only one. An interface (in languages like Java 8+) defines method contracts and can have default methods and static methods but traditionally no state. Use an abstract class when subclasses share common implementation; use an interface when unrelated classes need to share a contract. In Java 8+ and C#, interfaces with default methods blur the line, but interfaces still cannot hold instance state.",
      "difficulty": "easy",
      "tags": [
        "abstract-class",
        "interface",
        "oop-fundamentals"
      ]
    },
    {
      "id": "oop-008",
      "categoryId": "oop-design-patterns",
      "question": "Explain polymorphism with a practical example.",
      "answer": "Polymorphism lets you write code that works with a base type but executes different behavior depending on the actual subtype. For example, a render(Shape shape) method calls shape.draw() which dispatches to Circle.draw(), Rectangle.draw(), etc. at runtime. This eliminates switch/if-else chains on type. The two main forms are compile-time (method overloading, generics) and runtime (virtual method dispatch via inheritance or interface implementation).",
      "difficulty": "easy",
      "tags": [
        "polymorphism",
        "oop-fundamentals"
      ]
    },
    {
      "id": "oop-009",
      "categoryId": "oop-design-patterns",
      "question": "What is the Singleton pattern and why is it often considered an anti-pattern?",
      "answer": "Singleton ensures a class has exactly one instance globally, typically via a private constructor and static getInstance() method. It is useful for truly global resources like a connection pool or logger. However, it introduces hidden global state, makes unit testing difficult (hard to mock), tightly couples consumers to the concrete class, and causes issues in concurrent and distributed systems. Modern best practice is to use dependency injection with a singleton scope instead, which gives the same single-instance behavior without the downsides.",
      "difficulty": "medium",
      "tags": [
        "singleton",
        "creational-patterns",
        "anti-patterns"
      ]
    },
    {
      "id": "oop-010",
      "categoryId": "oop-design-patterns",
      "question": "Explain the Factory pattern and when you would use it.",
      "answer": "Factory encapsulates object creation logic so callers do not need to know which concrete class to instantiate. A NotificationFactory.create('email') returns an EmailNotification while create('sms') returns an SmsNotification, both implementing a Notification interface. Use it when construction logic is complex, when the exact type depends on runtime data, or when you want to centralize and control how objects are created. Abstract Factory extends this to families of related objects.",
      "difficulty": "easy",
      "tags": [
        "factory",
        "creational-patterns"
      ]
    },
    {
      "id": "oop-011",
      "categoryId": "oop-design-patterns",
      "question": "How does the Observer pattern work and where is it commonly used?",
      "answer": "Observer defines a one-to-many relationship where a subject maintains a list of observers and notifies them when its state changes, typically by calling an update() method on each observer. Observers register and unregister themselves. It is the backbone of event systems in UI frameworks (DOM event listeners, React state subscriptions), message brokers, and reactive programming (RxJS Observables). The key benefit is loose coupling: the subject does not know or care what observers do with the notification.",
      "difficulty": "medium",
      "tags": [
        "observer",
        "behavioral-patterns",
        "events"
      ]
    },
    {
      "id": "oop-012",
      "categoryId": "oop-design-patterns",
      "question": "Explain the Strategy pattern with an example.",
      "answer": "Strategy lets you define a family of algorithms, encapsulate each one, and swap them at runtime. For example, a ShippingCalculator takes a ShippingStrategy (FedExStrategy, UPSStrategy, FlatRateStrategy) and delegates cost calculation to it. The client picks the strategy based on context. This replaces complex if/else or switch blocks for selecting algorithms. It follows OCP because adding a new strategy requires no changes to existing code. Java's Comparator and sorting functions are real-world Strategy implementations.",
      "difficulty": "medium",
      "tags": [
        "strategy",
        "behavioral-patterns"
      ]
    },
    {
      "id": "oop-013",
      "categoryId": "oop-design-patterns",
      "question": "What is the Decorator pattern and how does it differ from inheritance?",
      "answer": "Decorator wraps an object to add behavior dynamically without altering the original class. Each decorator implements the same interface as the wrapped object. For example, a base FileReader can be wrapped with BufferedReader, then EncryptedReader, each adding functionality. Unlike inheritance (static, one dimension), decorators compose at runtime and can stack in any combination. Java I/O streams are the classic example: new BufferedInputStream(new FileInputStream(file)).",
      "difficulty": "hard",
      "tags": [
        "decorator",
        "structural-patterns",
        "composition"
      ]
    },
    {
      "id": "oop-014",
      "categoryId": "oop-design-patterns",
      "question": "Explain the Adapter pattern and give a practical scenario.",
      "answer": "Adapter converts the interface of an existing class into one that a client expects, enabling incompatible classes to work together. For example, your app expects a PaymentProcessor interface with charge(amount), but a third-party library has StripeAPI with createPayment(cents, currency). You write a StripeAdapter implementing PaymentProcessor that translates charge() into createPayment(). This avoids modifying third-party code and insulates your app from vendor-specific APIs.",
      "difficulty": "medium",
      "tags": [
        "adapter",
        "structural-patterns",
        "integration"
      ]
    },
    {
      "id": "oop-015",
      "categoryId": "oop-design-patterns",
      "question": "When would you use the Builder pattern?",
      "answer": "Use Builder when constructing an object requires many optional parameters, making constructor overloading or telescoping constructors unreadable. For example, HttpRequest.builder().url(u).method('POST').header('Auth', token).body(data).timeout(5000).build(). Builder enforces required vs optional fields, allows fluent chaining, and makes the code self-documenting. It also enables immutable objects since the builder accumulates state then constructs the final object in one shot.",
      "difficulty": "easy",
      "tags": [
        "builder",
        "creational-patterns",
        "fluent-api"
      ]
    },
    {
      "id": "oop-016",
      "categoryId": "oop-design-patterns",
      "question": "What is dependency injection and what are the three common forms?",
      "answer": "Dependency injection is supplying a class's dependencies from outside rather than having the class create them internally. The three forms are constructor injection (dependencies passed via constructor, preferred because it makes them explicit and enables immutability), setter injection (via setter methods, useful for optional dependencies), and interface injection (the class implements an injector interface, rare in practice). Frameworks like Spring, Guice, and Angular automate this with IoC containers that wire dependencies based on configuration or annotations.",
      "difficulty": "medium",
      "tags": [
        "dependency-injection",
        "ioc",
        "design-principles"
      ]
    },
    {
      "id": "oop-017",
      "categoryId": "oop-design-patterns",
      "question": "Explain coupling and cohesion and their relationship to good design.",
      "answer": "Coupling measures how dependent modules are on each other; low coupling means changes in one module rarely require changes in another. Cohesion measures how focused a module's responsibilities are; high cohesion means everything in the module relates to a single purpose. Good design targets low coupling and high cohesion. A UserService that handles auth, profile updates, and email sending has low cohesion. Splitting into AuthService, ProfileService, and EmailService increases cohesion and typically reduces coupling between unrelated concerns.",
      "difficulty": "easy",
      "tags": [
        "coupling",
        "cohesion",
        "design-principles"
      ]
    },
    {
      "id": "oop-018",
      "categoryId": "oop-design-patterns",
      "question": "What do KISS, DRY, and YAGNI mean and how do they guide decisions?",
      "answer": "KISS (Keep It Simple, Stupid) says prefer the simplest solution that works; complexity is a liability. DRY (Don't Repeat Yourself) says extract shared logic to avoid duplicating knowledge, but only when duplication represents the same concept, not just similar-looking code. YAGNI (You Aren't Gonna Need It) says do not build features or abstractions until they are actually needed. These three together prevent over-engineering: DRY without YAGNI leads to premature abstractions, and KISS keeps you from building clever systems nobody asked for.",
      "difficulty": "easy",
      "tags": [
        "kiss",
        "dry",
        "yagni",
        "design-principles"
      ]
    },
    {
      "id": "oop-019",
      "categoryId": "oop-design-patterns",
      "question": "Compare MVC and MVVM architectures.",
      "answer": "MVC (Model-View-Controller) has the Controller receive user input, update the Model, and select a View. The View reads from the Model to render. It is common in server-side frameworks like Rails and Spring MVC. MVVM (Model-View-ViewModel) has the View data-bind to a ViewModel that exposes observable properties. User interactions update the ViewModel, which updates the Model. The two-way binding eliminates manual DOM manipulation. MVVM is dominant in frontend frameworks (React/Vue with state management, WPF, SwiftUI).",
      "difficulty": "hard",
      "tags": [
        "mvc",
        "mvvm",
        "architecture"
      ]
    },
    {
      "id": "oop-020",
      "categoryId": "oop-design-patterns",
      "question": "Explain the State pattern and when it is better than if/else chains.",
      "answer": "The State pattern lets an object alter its behavior when its internal state changes, as if the object changed its class. Each state is a separate class implementing a common State interface. For example, a VendingMachine has IdleState, HasMoneyState, DispensingState, each handling insertCoin(), selectItem(), and dispense() differently. Transitions are explicit. Use it when an object has more than 3-4 states with distinct behaviors; if/else chains become unreadable, hard to extend, and prone to bugs when adding new states or transitions.",
      "difficulty": "hard",
      "tags": [
        "state-pattern",
        "behavioral-patterns",
        "finite-state-machine"
      ]
    },
    {
      "id": "js-001",
      "categoryId": "javascript",
      "question": "What is a closure in JavaScript and why are they useful?",
      "answer": "A closure is a function that retains access to its outer (lexical) scope variables even after the outer function has returned. When you define a function inside another function, the inner function closes over the outer variables. Closures enable data privacy (module pattern), partial application, and factories. For example, function counter() { let n = 0; return () => ++n; } creates a private n that only the returned function can access.",
      "difficulty": "easy",
      "tags": [
        "closures",
        "scope",
        "functions"
      ]
    },
    {
      "id": "js-002",
      "categoryId": "javascript",
      "question": "Explain hoisting in JavaScript. How do var, let, and const differ?",
      "answer": "Hoisting moves declarations (not assignments) to the top of their scope during compilation. var declarations are hoisted and initialized to undefined, so referencing them before assignment gives undefined. let and const are hoisted but placed in a Temporal Dead Zone (TDZ) from scope entry until the declaration line, so accessing them before declaration throws a ReferenceError. Function declarations are fully hoisted (body included), while function expressions are hoisted as variables following var/let/const rules.",
      "difficulty": "medium",
      "tags": [
        "hoisting",
        "var",
        "let",
        "const"
      ]
    },
    {
      "id": "js-003",
      "categoryId": "javascript",
      "question": "Describe the JavaScript event loop and how it handles asynchronous code.",
      "answer": "JavaScript is single-threaded with a call stack for synchronous code. Async operations (timers, I/O, fetch) are offloaded to browser/Node APIs. When complete, their callbacks go to the task queue (macrotasks) or microtask queue (Promises, queueMicrotask). The event loop checks: if the call stack is empty, drain all microtasks first, then take one macrotask and execute it. This is why Promise.then callbacks run before setTimeout(fn, 0) even though both are queued.",
      "difficulty": "hard",
      "tags": [
        "event-loop",
        "async",
        "microtasks",
        "macrotasks"
      ]
    },
    {
      "id": "js-004",
      "categoryId": "javascript",
      "question": "How does the prototype chain work in JavaScript?",
      "answer": "Every object has an internal [[Prototype]] link (accessible via __proto__ or Object.getPrototypeOf). When you access a property that does not exist on the object, the engine walks up the prototype chain through each linked prototype until it finds the property or reaches null (end of chain). Constructor functions have a .prototype property that becomes the [[Prototype]] of instances created with new. This is how methods defined on Array.prototype are available on all array instances without being copied to each one.",
      "difficulty": "medium",
      "tags": [
        "prototype",
        "prototype-chain",
        "inheritance"
      ]
    },
    {
      "id": "js-005",
      "categoryId": "javascript",
      "question": "Explain how the 'this' keyword works in different contexts.",
      "answer": "In a regular function call, this is the global object (or undefined in strict mode). As an object method, this is the calling object. With new, this is the newly created instance. call/apply/bind explicitly set this. Arrow functions have no own this; they capture this from the enclosing lexical scope at definition time. Event handlers in the DOM set this to the element, unless using an arrow function. The most common bug is losing this when passing a method as a callback, which bind or arrow functions fix.",
      "difficulty": "medium",
      "tags": [
        "this",
        "context",
        "binding"
      ]
    },
    {
      "id": "js-006",
      "categoryId": "javascript",
      "question": "How do arrow functions differ from regular functions beyond syntax?",
      "answer": "Arrow functions do not have their own this, arguments, super, or new.target. They lexically inherit this from the enclosing scope, which is why they are ideal for callbacks and closures where you want to preserve the outer this. They cannot be used as constructors (new throws TypeError). They also do not have a prototype property. Regular functions get their this from how they are called, have an arguments object, and can be used with new.",
      "difficulty": "easy",
      "tags": [
        "arrow-functions",
        "this",
        "es6"
      ]
    },
    {
      "id": "js-007",
      "categoryId": "javascript",
      "question": "Compare Promises and callbacks for handling asynchronous code.",
      "answer": "Callbacks pass a function to be called when an async operation completes, but nested callbacks create 'callback hell' and make error handling inconsistent (each callback needs its own error check). Promises represent a future value with .then() for success and .catch() for errors, enabling chaining and centralized error handling. Promises also solve inversion of control: you control when to process the result, rather than trusting the callee to call your callback correctly and only once.",
      "difficulty": "easy",
      "tags": [
        "promises",
        "callbacks",
        "async"
      ]
    },
    {
      "id": "js-008",
      "categoryId": "javascript",
      "question": "How does async/await work under the hood?",
      "answer": "Async functions always return a Promise. The await keyword pauses execution of the async function at that point, yields control back to the caller, and resumes when the awaited Promise settles. Under the hood, the engine transforms it into a state machine similar to generators. await is syntactic sugar over Promise.then chains. An important gotcha: awaiting in a loop is sequential; use Promise.all for parallel execution. Errors from rejected Promises can be caught with try/catch, which makes async error handling read like synchronous code.",
      "difficulty": "medium",
      "tags": [
        "async-await",
        "promises",
        "es2017"
      ]
    },
    {
      "id": "js-009",
      "categoryId": "javascript",
      "question": "What are generators in JavaScript and when would you use them?",
      "answer": "Generators are functions declared with function* that can pause and resume via yield. Calling a generator returns an iterator; each .next() call runs until the next yield, returning {value, done}. They enable lazy evaluation (generate infinite sequences without consuming memory), custom iterables, and cooperative multitasking. Before async/await, libraries like co used generators with Promises for async control flow. They are also useful for building streaming data pipelines and state machines.",
      "difficulty": "hard",
      "tags": [
        "generators",
        "iterators",
        "lazy-evaluation"
      ]
    },
    {
      "id": "js-010",
      "categoryId": "javascript",
      "question": "Explain the differences between Map, Set, and WeakMap.",
      "answer": "Map is a key-value collection where keys can be any type (not just strings like plain objects), maintains insertion order, and has O(1) lookup. Set stores unique values of any type with O(1) add/has/delete. WeakMap only accepts objects as keys and holds weak references, meaning if the key object has no other references, it can be garbage collected. WeakMap is not iterable and has no size property. Use WeakMap for associating metadata with objects without preventing their garbage collection, like caching or private data.",
      "difficulty": "medium",
      "tags": [
        "map",
        "set",
        "weakmap",
        "data-structures"
      ]
    },
    {
      "id": "js-011",
      "categoryId": "javascript",
      "question": "Explain destructuring with nested objects and default values.",
      "answer": "Destructuring extracts values from objects and arrays into variables. For nested objects: const { user: { name, age = 25 } } = response; extracts name and age from response.user with a default of 25 for age. For arrays: const [first, , third] = arr; skips the second element. You can rename: { name: userName } extracts name as userName. Combined with rest: const { id, ...rest } = obj; collects remaining properties. Default values only apply when the value is undefined, not null.",
      "difficulty": "easy",
      "tags": [
        "destructuring",
        "es6",
        "syntax"
      ]
    },
    {
      "id": "js-012",
      "categoryId": "javascript",
      "question": "What is the spread operator and how does rest differ from it?",
      "answer": "Spread (...) expands an iterable into individual elements: [...arr1, ...arr2] merges arrays, {...obj1, ...obj2} merges objects (shallow copy). Rest (...) collects remaining elements into an array or object: function sum(...nums) collects all arguments, const {a, ...rest} = obj collects remaining properties. They look identical but spread unpacks and rest packs. Important: spread on objects is a shallow clone; nested objects are still references. Object spread also only copies enumerable own properties.",
      "difficulty": "easy",
      "tags": [
        "spread",
        "rest",
        "es6"
      ]
    },
    {
      "id": "js-013",
      "categoryId": "javascript",
      "question": "Compare ES6 modules and CommonJS modules.",
      "answer": "ES6 modules use import/export with static analysis at compile time, enabling tree-shaking (dead code elimination). They are strict mode by default and have live bindings (importing a variable reflects changes in the exporter). CommonJS uses require/module.exports, is evaluated at runtime (dynamic), and produces a copy of the exported value at require time. CommonJS is synchronous, designed for Node.js. ES6 modules are the standard, supported in browsers natively and in Node.js (with .mjs extension or type:module in package.json).",
      "difficulty": "medium",
      "tags": [
        "modules",
        "esm",
        "commonjs",
        "imports"
      ]
    },
    {
      "id": "js-014",
      "categoryId": "javascript",
      "question": "How do typeof and instanceof differ and what are their pitfalls?",
      "answer": "typeof returns a string like 'number', 'string', 'object', 'function', 'undefined'. Its pitfalls: typeof null is 'object' (historical bug), typeof [] is 'object', and typeof NaN is 'number'. instanceof checks the prototype chain: [] instanceof Array is true. Its pitfalls: it fails across iframes/realms (different Array constructors), does not work for primitives (5 instanceof Number is false), and can be fooled by manually setting prototypes. For reliable type checking, use Object.prototype.toString.call(val).",
      "difficulty": "medium",
      "tags": [
        "typeof",
        "instanceof",
        "type-checking"
      ]
    },
    {
      "id": "js-015",
      "categoryId": "javascript",
      "question": "Explain JavaScript type coercion with examples of tricky cases.",
      "answer": "JavaScript implicitly converts types in operations: '5' + 3 gives '53' (string concatenation wins), '5' - 3 gives 2 (subtraction coerces to number). == uses coercion (null == undefined is true, '' == 0 is true), while === does not. Boolean coercion: 0, '', null, undefined, NaN, and false are falsy; everything else is truthy including '0' and []. The + unary operator converts to number: +'5' gives 5. Always use === and explicit conversions (Number(), String()) to avoid bugs.",
      "difficulty": "medium",
      "tags": [
        "coercion",
        "type-conversion",
        "equality"
      ]
    },
    {
      "id": "js-016",
      "categoryId": "javascript",
      "question": "Implement and explain debounce vs throttle.",
      "answer": "Debounce delays execution until a pause in calls: function debounce(fn, ms) { let timer; return (...args) => { clearTimeout(timer); timer = setTimeout(() => fn(...args), ms); }; } It fires only after the caller stops for ms milliseconds, ideal for search input. Throttle ensures at most one execution per interval: it runs immediately then ignores calls until the interval passes. Use debounce for 'wait until done' (search, resize end), throttle for 'run at most every N ms' (scroll, mousemove).",
      "difficulty": "hard",
      "tags": [
        "debounce",
        "throttle",
        "performance",
        "event-handling"
      ]
    },
    {
      "id": "js-017",
      "categoryId": "javascript",
      "question": "What are Proxy and Reflect in JavaScript?",
      "answer": "Proxy wraps an object and intercepts fundamental operations (get, set, has, deleteProperty, apply, construct, etc.) via handler traps. For example, new Proxy(target, { get(obj, prop) { log(prop); return Reflect.get(obj, prop); } }) logs every property access. Reflect provides the default behavior for each trap as static methods, serving as the standard way to forward operations from within a trap. Proxies enable validation, observable objects, auto-populating defaults, API mocking, and Vue 3's reactivity system.",
      "difficulty": "hard",
      "tags": [
        "proxy",
        "reflect",
        "metaprogramming"
      ]
    },
    {
      "id": "js-018",
      "categoryId": "javascript",
      "question": "What is a Symbol and what problems does it solve?",
      "answer": "Symbol is a primitive type that creates unique, immutable identifiers. Every Symbol() call produces a value guaranteed to never collide with any other value, including other Symbols. This makes them ideal for object property keys that will not clash with user-defined keys or future language additions. Well-known Symbols like Symbol.iterator, Symbol.toPrimitive, and Symbol.hasInstance let you customize built-in behavior. Symbol.for('key') creates globally shared Symbols via a registry. Symbols are not enumerated by for...in or Object.keys().",
      "difficulty": "hard",
      "tags": [
        "symbol",
        "metaprogramming",
        "es6"
      ]
    },
    {
      "id": "js-019",
      "categoryId": "javascript",
      "question": "How do iterators and the iterable protocol work in JavaScript?",
      "answer": "An iterable is any object with a [Symbol.iterator]() method that returns an iterator. An iterator has a next() method that returns {value, done}. Arrays, strings, Maps, and Sets are built-in iterables. The for...of loop, spread operator, destructuring, and Promise.all all consume iterables. You can make any object iterable by implementing [Symbol.iterator]. Generators automatically satisfy both protocols. This is a pull-based model where the consumer requests values on demand, enabling lazy evaluation.",
      "difficulty": "medium",
      "tags": [
        "iterators",
        "iterable-protocol",
        "symbol-iterator"
      ]
    },
    {
      "id": "js-020",
      "categoryId": "javascript",
      "question": "What is the difference between undefined and null, and when do you use each?",
      "answer": "undefined means a variable was declared but not assigned, a function parameter was not passed, or a property does not exist. null is an intentional assignment meaning 'no value.' typeof undefined is 'undefined', typeof null is 'object' (legacy bug). null == undefined is true (loose equality), but null === undefined is false. Convention: use null when you want to explicitly clear a variable or signal 'no result' (e.g., getElementById returns null). Never assign undefined manually; let JavaScript use it for its intended meaning of 'not yet defined.'",
      "difficulty": "easy",
      "tags": [
        "undefined",
        "null",
        "primitives"
      ]
    },
    {
      "id": "py-001",
      "categoryId": "python",
      "question": "What is the GIL in Python and how does it affect multithreading?",
      "answer": "The Global Interpreter Lock is a mutex in CPython that allows only one thread to execute Python bytecode at a time, even on multi-core machines. This means CPU-bound multithreaded code gets no parallel speedup and may even be slower due to lock contention. I/O-bound threads still benefit because the GIL is released during I/O waits (network, file, sleep). For CPU-bound parallelism, use multiprocessing (separate processes with separate GILs), C extensions that release the GIL, or alternative interpreters. Python 3.13 introduced an experimental free-threaded mode (no GIL).",
      "difficulty": "hard",
      "tags": [
        "gil",
        "threading",
        "cpython",
        "concurrency"
      ]
    },
    {
      "id": "py-002",
      "categoryId": "python",
      "question": "How do generators work in Python and what is the benefit of yield?",
      "answer": "A generator function uses yield instead of return. When called, it returns a generator object without executing the body. Each next() call executes until the next yield, suspends the function's state, and returns the yielded value. This enables lazy evaluation: processing one item at a time without loading everything into memory. For example, reading a 10GB file line by line with a generator uses constant memory. yield from delegates to a sub-generator. Generators are the foundation for Python's asyncio coroutines.",
      "difficulty": "medium",
      "tags": [
        "generators",
        "yield",
        "lazy-evaluation",
        "iterators"
      ]
    },
    {
      "id": "py-003",
      "categoryId": "python",
      "question": "Explain Python decorators and write a simple example.",
      "answer": "A decorator is a function that takes a function and returns a modified version of it. The @decorator syntax is sugar for func = decorator(func). Example: def timer(fn): def wrapper(*args, **kwargs): start = time.time(); result = fn(*args, **kwargs); print(f'{fn.__name__} took {time.time()-start:.2f}s'); return result; return wrapper. Use @functools.wraps(fn) on the wrapper to preserve the original function's name and docstring. Decorators are used heavily for logging, auth, caching (@lru_cache), rate limiting, and retry logic.",
      "difficulty": "medium",
      "tags": [
        "decorators",
        "functions",
        "metaprogramming"
      ]
    },
    {
      "id": "py-004",
      "categoryId": "python",
      "question": "What are list comprehensions and when should you avoid them?",
      "answer": "List comprehensions are concise syntax for creating lists: [x**2 for x in range(10) if x % 2 == 0]. They are generally faster than equivalent for loops because the iteration happens in optimized C code. Avoid them when the logic is complex enough to hurt readability (nested comprehensions with conditions), when you need side effects (use a loop instead), or when memory is a concern for large datasets (use a generator expression with parentheses instead). They also support nested loops: [(x,y) for x in a for y in b].",
      "difficulty": "easy",
      "tags": [
        "list-comprehensions",
        "syntax",
        "performance"
      ]
    },
    {
      "id": "py-005",
      "categoryId": "python",
      "question": "Explain *args and **kwargs with a practical use case.",
      "answer": "*args collects positional arguments into a tuple, **kwargs collects keyword arguments into a dict. Together they let a function accept any number of arguments. Practical use: writing decorators that wrap arbitrary functions: def log(fn): def wrapper(*args, **kwargs): print(f'Calling {fn.__name__}'); return fn(*args, **kwargs); return wrapper. You can also unpack with them: func(*list_of_args, **dict_of_kwargs). In function signatures, named parameters can appear between *args and **kwargs to create keyword-only arguments.",
      "difficulty": "easy",
      "tags": [
        "args",
        "kwargs",
        "functions",
        "unpacking"
      ]
    },
    {
      "id": "py-006",
      "categoryId": "python",
      "question": "What are dunder methods and name the most important ones?",
      "answer": "Dunder (double underscore) methods are special methods Python calls implicitly for operations. __init__ initializes instances, __repr__ provides a developer-readable string, __str__ provides a user-readable string. __eq__ and __hash__ enable equality checks and dictionary keys. __len__, __getitem__, __iter__, __contains__ make objects behave like collections. __enter__/__exit__ enable context managers. __call__ makes instances callable. __add__, __lt__, etc. enable operator overloading. Implementing the right dunders lets your objects work naturally with Python's syntax and built-in functions.",
      "difficulty": "medium",
      "tags": [
        "dunder-methods",
        "magic-methods",
        "oop"
      ]
    },
    {
      "id": "py-007",
      "categoryId": "python",
      "question": "How do context managers work and when would you write a custom one?",
      "answer": "Context managers implement __enter__ (called on with entry, return value bound to 'as') and __exit__ (called on exit, receives exception info, can suppress exceptions by returning True). They guarantee cleanup even if exceptions occur. The easier way is @contextlib.contextmanager: yield in a try/finally separates setup from teardown. Write custom ones for database transactions (commit or rollback), temporary state changes (cd into a directory then restore), acquiring/releasing locks, or any resource that needs deterministic cleanup beyond just closing a file.",
      "difficulty": "medium",
      "tags": [
        "context-managers",
        "with-statement",
        "resource-management"
      ]
    },
    {
      "id": "py-008",
      "categoryId": "python",
      "question": "Explain Python's asyncio and how it differs from threading.",
      "answer": "asyncio uses cooperative multitasking with a single-threaded event loop. Coroutines (async def) voluntarily yield control with await, allowing other coroutines to run during I/O waits. Threading uses OS threads with preemptive switching and the GIL limiting true parallelism. asyncio scales to thousands of concurrent connections with low overhead (coroutines are ~1KB vs ~8MB per thread), has no race conditions on shared state (single thread), and avoids the GIL bottleneck. Use asyncio for high-concurrency I/O (web servers, scrapers); use threading for integrating blocking libraries.",
      "difficulty": "hard",
      "tags": [
        "asyncio",
        "async",
        "concurrency",
        "event-loop"
      ]
    },
    {
      "id": "py-009",
      "categoryId": "python",
      "question": "How do type hints work in Python and are they enforced at runtime?",
      "answer": "Type hints (PEP 484) are annotations that indicate expected types: def greet(name: str) -> str. They are NOT enforced at runtime by default; Python ignores them during execution. Their value comes from static analysis tools (mypy, pyright) that catch type errors before running code, and from IDE features like autocomplete and refactoring. You can use Union, Optional, List, Dict, Tuple from typing, or in Python 3.10+ use the | syntax (str | None). Runtime enforcement is possible via libraries like pydantic or beartype, which inspect annotations.",
      "difficulty": "easy",
      "tags": [
        "type-hints",
        "typing",
        "static-analysis"
      ]
    },
    {
      "id": "py-010",
      "categoryId": "python",
      "question": "What are dataclasses and how do they compare to regular classes and NamedTuples?",
      "answer": "@dataclass auto-generates __init__, __repr__, __eq__, and optionally __hash__, __order__ based on annotated fields. Compared to regular classes, they eliminate boilerplate for data-holding classes. Compared to NamedTuples, dataclasses are mutable by default (frozen=True for immutable), support default values and field factories, allow inheritance, and can have methods. NamedTuples are tuples (immutable, iterable, unpackable, less memory). Use dataclasses for mutable domain objects, NamedTuples for lightweight immutable records, and pydantic BaseModel when you need validation.",
      "difficulty": "medium",
      "tags": [
        "dataclasses",
        "namedtuple",
        "data-modeling"
      ]
    },
    {
      "id": "py-011",
      "categoryId": "python",
      "question": "What are __slots__ and when should you use them?",
      "answer": "By default, Python objects store attributes in a per-instance __dict__ dictionary. Defining __slots__ = ('x', 'y') in a class replaces the dict with a fixed-size struct-like layout, reducing memory usage by 30-50% per instance and slightly speeding up attribute access. The tradeoff: you cannot add arbitrary attributes at runtime, and you must redeclare slots in subclasses. Use __slots__ when creating millions of instances (ORM rows, data points, game entities) where memory matters. Do not bother for classes with few instances.",
      "difficulty": "hard",
      "tags": [
        "slots",
        "memory-optimization",
        "performance"
      ]
    },
    {
      "id": "py-012",
      "categoryId": "python",
      "question": "What are metaclasses in Python and when would you actually use one?",
      "answer": "A metaclass is the class of a class. When Python encounters a class statement, it calls the metaclass (default: type) to create the class object. A custom metaclass overrides __new__ or __init__ on type to intercept class creation, allowing you to modify or validate the class before it exists. Real use cases: Django models (ModelBase metaclass builds field mappings), ABCs (ABCMeta enforces abstract methods), ORMs (mapping classes to tables), and API registration (auto-registering subclasses in a registry). For most cases, class decorators or __init_subclass__ are simpler alternatives.",
      "difficulty": "hard",
      "tags": [
        "metaclasses",
        "metaprogramming",
        "type-system"
      ]
    },
    {
      "id": "py-013",
      "categoryId": "python",
      "question": "Compare multiprocessing and threading in Python for CPU-bound tasks.",
      "answer": "For CPU-bound work, multiprocessing is the correct choice because each process has its own Python interpreter and GIL, enabling true parallelism across cores. Threading cannot achieve CPU parallelism due to the GIL. However, multiprocessing has higher overhead: each process copies memory (or use shared memory), IPC is needed for communication (Queue, Pipe), and spawning processes takes ~50-100ms vs ~1ms for threads. Use concurrent.futures.ProcessPoolExecutor for the simplest API. For I/O-bound tasks, threading or asyncio is preferred due to lower overhead.",
      "difficulty": "medium",
      "tags": [
        "multiprocessing",
        "threading",
        "concurrency",
        "parallelism"
      ]
    },
    {
      "id": "py-014",
      "categoryId": "python",
      "question": "What is the walrus operator and when is it useful?",
      "answer": "The walrus operator := (PEP 572, Python 3.8+) assigns a value to a variable as part of an expression. For example: if (n := len(items)) > 10: print(f'Too many: {n}'). Without it, you would need a separate assignment line. It shines in while loops (while chunk := f.read(8192):), list comprehensions with expensive computations ([y for x in data if (y := transform(x)) > threshold]), and regex matching (if m := re.match(pattern, text):). Use it sparingly; overuse hurts readability.",
      "difficulty": "medium",
      "tags": [
        "walrus-operator",
        "assignment-expressions",
        "python38"
      ]
    },
    {
      "id": "py-015",
      "categoryId": "python",
      "question": "How do dict comprehensions work and how do they differ from dict() constructor calls?",
      "answer": "Dict comprehensions create dictionaries with {key_expr: val_expr for item in iterable if condition}. Example: {word: len(word) for word in words} or inverting a dict: {v: k for k, v in mapping.items()}. Compared to dict() with a generator of tuples, comprehensions are faster (optimized bytecode, avoids tuple creation) and more readable. They can also merge and transform: {k: v.upper() for d in dicts for k, v in d.items()}. Like list comprehensions, avoid them when logic gets complex enough to hurt readability.",
      "difficulty": "easy",
      "tags": [
        "dict-comprehensions",
        "dictionaries",
        "syntax"
      ]
    },
    {
      "id": "java-001",
      "categoryId": "java",
      "question": "Explain the JVM memory model: heap vs stack.",
      "answer": "The stack stores method frames: local variables, parameters, and return addresses. Each thread gets its own stack (default ~512KB-1MB). The heap is shared memory where all objects and arrays live, managed by the garbage collector. The heap is divided into Young Generation (Eden + two Survivor spaces for short-lived objects) and Old Generation (for long-lived objects). Method references and class metadata live in Metaspace (native memory, not heap, since Java 8). Stack access is LIFO and fast; heap access requires pointer dereferencing and GC overhead.",
      "difficulty": "medium",
      "tags": [
        "jvm",
        "memory-model",
        "heap",
        "stack"
      ]
    },
    {
      "id": "java-002",
      "categoryId": "java",
      "question": "How does garbage collection work in Java and what are the main collectors?",
      "answer": "The GC automatically reclaims memory from unreachable objects. Young Generation uses minor GC (fast, frequent): objects in Eden are copied to Survivor if alive, eventually promoted to Old Gen. Major/Full GC collects Old Gen (slower, causes pauses). Key collectors: G1 (default since Java 9, targets pause-time goals, divides heap into regions), ZGC (sub-millisecond pauses even on terabyte heaps, concurrent), and Shenandoah (similar to ZGC, concurrent compaction). Choose G1 for general use, ZGC for latency-critical apps. Tune with -Xms, -Xmx, and -XX:MaxGCPauseMillis.",
      "difficulty": "hard",
      "tags": [
        "garbage-collection",
        "jvm",
        "g1",
        "zgc"
      ]
    },
    {
      "id": "java-003",
      "categoryId": "java",
      "question": "What are generics in Java and what is type erasure?",
      "answer": "Generics add compile-time type safety to collections and classes: List<String> prevents adding an Integer. Type erasure means generic type information is removed at runtime; List<String> and List<Integer> are both just List at the bytecode level. This means you cannot do new T(), instanceof List<String>, or create generic arrays. Bounded types (T extends Comparable<T>) constrain what types are allowed. Wildcards (? extends Number, ? super Integer) follow PECS: Producer Extends, Consumer Super. Erasure exists for backward compatibility with pre-Java-5 code.",
      "difficulty": "hard",
      "tags": [
        "generics",
        "type-erasure",
        "type-safety"
      ]
    },
    {
      "id": "java-004",
      "categoryId": "java",
      "question": "How does the Streams API work and when should you use parallel streams?",
      "answer": "Streams provide a functional pipeline for processing collections: source.filter().map().collect(). They are lazy (intermediate operations build a pipeline, terminal operations trigger execution) and can be parallel. A stream is used once (single traversal). Key terminal operations: collect, reduce, forEach, count, findFirst. Use parallel streams only for CPU-bound operations on large datasets (10,000+ elements) with stateless, non-interfering operations. Parallel streams use the common ForkJoinPool, so blocking operations inside them can starve other parallel streams in the same JVM.",
      "difficulty": "medium",
      "tags": [
        "streams",
        "functional-programming",
        "parallel-streams"
      ]
    },
    {
      "id": "java-005",
      "categoryId": "java",
      "question": "What is a functional interface and how does it relate to lambdas?",
      "answer": "A functional interface has exactly one abstract method, annotated with @FunctionalInterface. Lambda expressions are anonymous implementations of functional interfaces: (x, y) -> x + y implements a BiFunction. Key built-in interfaces: Function<T,R> (transform), Predicate<T> (test), Consumer<T> (accept, no return), Supplier<T> (get, no input), BiFunction, UnaryOperator. Method references (String::toLowerCase) are shorthand lambdas. Lambdas enable passing behavior as parameters, powering the Streams API, CompletableFuture callbacks, and event handlers.",
      "difficulty": "easy",
      "tags": [
        "functional-interfaces",
        "lambdas",
        "java8"
      ]
    },
    {
      "id": "java-006",
      "categoryId": "java",
      "question": "How does CompletableFuture work for async programming in Java?",
      "answer": "CompletableFuture represents a value that will be available later and provides a fluent API for composing async operations. supplyAsync runs a task in ForkJoinPool and returns a CompletableFuture. Chain with thenApply (transform), thenCompose (flatMap another future), thenCombine (merge two futures), and thenAccept (consume result). Exception handling: exceptionally or handle. allOf waits for multiple futures. Unlike Future.get() which blocks, CompletableFuture enables non-blocking composition. Use a custom executor (not the common pool) for I/O-heavy tasks to avoid pool exhaustion.",
      "difficulty": "hard",
      "tags": [
        "completable-future",
        "async",
        "concurrency"
      ]
    },
    {
      "id": "java-007",
      "categoryId": "java",
      "question": "Explain synchronized and volatile in Java concurrency.",
      "answer": "synchronized provides mutual exclusion and memory visibility: only one thread can execute a synchronized block on the same monitor at a time, and changes are flushed to main memory on exit. volatile guarantees visibility (reads always see the latest write across threads) and prevents instruction reordering, but does NOT provide atomicity for compound operations like count++. Use volatile for flags and single-variable state. Use synchronized (or java.util.concurrent locks) when multiple variables must be updated atomically. Prefer higher-level constructs like AtomicInteger, ConcurrentHashMap, and Lock when possible.",
      "difficulty": "hard",
      "tags": [
        "synchronized",
        "volatile",
        "concurrency",
        "thread-safety"
      ]
    },
    {
      "id": "java-008",
      "categoryId": "java",
      "question": "How does HashMap work internally in Java?",
      "answer": "HashMap stores entries in an array of buckets. The key's hashCode() is passed through a spread function, then masked to pick a bucket index. Collisions are handled by chaining: entries in the same bucket form a linked list, which converts to a balanced red-black tree when the list exceeds 8 entries (treeify threshold). Default initial capacity is 16 with load factor 0.75; when 75% full, it doubles in size and rehashes all entries. get/put are O(1) average, O(log n) worst case (tree). HashMap is not thread-safe; use ConcurrentHashMap for concurrent access.",
      "difficulty": "medium",
      "tags": [
        "hashmap",
        "data-structures",
        "hashing"
      ]
    },
    {
      "id": "java-009",
      "categoryId": "java",
      "question": "What is the equals/hashCode contract and what breaks if you violate it?",
      "answer": "The contract: if a.equals(b) is true, then a.hashCode() must equal b.hashCode(). The reverse is not required (hash collisions are allowed). If you override equals but not hashCode, two equal objects can land in different HashMap buckets, making them invisible to lookups. For example, a Set could contain two 'equal' objects, or map.get(key) returns null even though an equal key exists. Best practice: override both, use the same fields in both methods, and use Objects.hash() for consistent hash computation.",
      "difficulty": "medium",
      "tags": [
        "equals",
        "hashcode",
        "contract",
        "collections"
      ]
    },
    {
      "id": "java-010",
      "categoryId": "java",
      "question": "What is the difference between checked and unchecked exceptions?",
      "answer": "Checked exceptions (extend Exception but not RuntimeException) must be caught or declared in the method's throws clause; the compiler enforces this. Examples: IOException, SQLException. Unchecked exceptions (extend RuntimeException) do not require handling. Examples: NullPointerException, IllegalArgumentException. Checked exceptions model recoverable conditions (file not found, network timeout), while unchecked exceptions indicate programming bugs. Modern Java style tends to avoid checked exceptions in APIs because they clutter signatures and encourage empty catch blocks; Spring and many libraries wrap checked exceptions in runtime exceptions.",
      "difficulty": "easy",
      "tags": [
        "exceptions",
        "checked",
        "unchecked",
        "error-handling"
      ]
    },
    {
      "id": "java-011",
      "categoryId": "java",
      "question": "How does Spring dependency injection work?",
      "answer": "Spring's IoC container manages object creation and wiring. You annotate classes with @Component (or @Service, @Repository, @Controller), and Spring discovers them via component scanning. Dependencies are injected via @Autowired on constructors (preferred), fields, or setters. The container resolves dependencies by type; use @Qualifier to disambiguate. Bean scope defaults to singleton (one instance per container). Spring Boot auto-configuration uses @Conditional annotations to wire beans based on classpath, properties, and existing beans. Constructor injection makes dependencies explicit, supports immutability, and simplifies testing.",
      "difficulty": "medium",
      "tags": [
        "spring",
        "dependency-injection",
        "ioc"
      ]
    },
    {
      "id": "java-012",
      "categoryId": "java",
      "question": "How have interfaces changed since Java 8?",
      "answer": "Java 8 added default methods (concrete methods in interfaces, enabling backward-compatible API evolution) and static methods. Java 9 added private methods in interfaces for code reuse within default methods. This means interfaces can now contain significant implementation, blurring the line with abstract classes. The remaining differences: abstract classes can have constructors, instance state (fields), and a single inheritance chain; interfaces support multiple implementation inheritance. Use interfaces for defining contracts across unrelated types; use abstract classes when shared state or constructor logic is needed.",
      "difficulty": "medium",
      "tags": [
        "interfaces",
        "abstract-class",
        "java8",
        "default-methods"
      ]
    },
    {
      "id": "java-013",
      "categoryId": "java",
      "question": "What are records in Java and when should you use them?",
      "answer": "Records (Java 16+) are immutable data carriers: record Point(int x, int y) {} auto-generates a constructor, getters (x(), y()), equals, hashCode, and toString. They are final (cannot be extended) and their fields are final. Use records for DTOs, value objects, compound map keys, and any class whose identity is defined by its data. You can add custom methods, static fields, and compact constructors for validation. They replace verbose POJO boilerplate and are more concise than Lombok @Data while being a built-in language feature.",
      "difficulty": "easy",
      "tags": [
        "records",
        "java16",
        "immutability",
        "data-classes"
      ]
    },
    {
      "id": "java-014",
      "categoryId": "java",
      "question": "What are sealed classes and why were they added to Java?",
      "answer": "Sealed classes (Java 17) restrict which classes can extend them using the permits clause: sealed class Shape permits Circle, Rectangle, Triangle {}. Subclasses must be final, sealed, or non-sealed. This gives exhaustive type hierarchies: the compiler knows all possible subtypes, enabling pattern matching in switch expressions without a default case. They model algebraic data types (sum types) and are ideal for domain models where you want controlled inheritance, like AST nodes, protocol messages, or state machine states. Combined with records and pattern matching, they enable expressive type-safe designs.",
      "difficulty": "hard",
      "tags": [
        "sealed-classes",
        "java17",
        "pattern-matching",
        "type-hierarchy"
      ]
    },
    {
      "id": "java-015",
      "categoryId": "java",
      "question": "How does Optional work and what are the best practices for using it?",
      "answer": "Optional<T> is a container that either holds a value or is empty, introduced in Java 8 to make null-returning APIs explicit. Create with Optional.of(val), Optional.ofNullable(val), or Optional.empty(). Consume with orElse, orElseGet (lazy), orElseThrow, ifPresent, map, and flatMap. Best practices: use it as a return type for methods that might not produce a result, never use it for fields or method parameters (adds overhead and complexity), never call get() without checking (use orElseThrow instead), and do not use Optional to wrap collections (return an empty collection instead).",
      "difficulty": "easy",
      "tags": [
        "optional",
        "null-safety",
        "java8"
      ]
    },
    {
      "id": "cyber-001",
      "categoryId": "cybersecurity",
      "question": "What is SQL injection and how do you prevent it?",
      "answer": "SQL injection is an attack where malicious SQL statements are inserted into input fields that get concatenated into database queries, allowing attackers to read, modify, or delete data. Prevention involves using parameterized queries (prepared statements) instead of string concatenation, employing ORM frameworks that handle escaping automatically, validating and sanitizing all user input, and applying the principle of least privilege to database accounts. Input validation alone is insufficient because creative encoding can bypass filters.",
      "difficulty": "easy",
      "tags": [
        "owasp",
        "sql-injection",
        "input-validation"
      ]
    },
    {
      "id": "cyber-002",
      "categoryId": "cybersecurity",
      "question": "Explain the difference between symmetric and asymmetric encryption. When would you use each?",
      "answer": "Symmetric encryption uses a single shared key for both encryption and decryption (e.g., AES-256), making it fast but requiring secure key exchange. Asymmetric encryption uses a public/private key pair where data encrypted with one key can only be decrypted by the other (e.g., RSA, ECDSA), solving the key distribution problem but being significantly slower. In practice, TLS uses asymmetric encryption during the handshake to securely exchange a symmetric session key, then switches to symmetric encryption for the actual data transfer. This hybrid approach combines the security of asymmetric key exchange with the performance of symmetric encryption.",
      "difficulty": "easy",
      "tags": [
        "cryptography",
        "encryption",
        "tls"
      ]
    },
    {
      "id": "cyber-003",
      "categoryId": "cybersecurity",
      "question": "What is Cross-Site Scripting (XSS) and what are its main types?",
      "answer": "XSS is an injection attack where malicious scripts are inserted into trusted websites and executed in victims' browsers. Stored XSS persists the payload in the server's database (e.g., a malicious comment) and fires for every user who views it. Reflected XSS embeds the payload in a URL parameter that the server echoes back without sanitization. DOM-based XSS occurs entirely client-side when JavaScript reads attacker-controlled data (like location.hash) and writes it to the DOM unsafely. Prevention includes output encoding (HTML-entity escaping), Content Security Policy headers, using frameworks that auto-escape by default (React, Angular), and never using innerHTML or eval with untrusted data.",
      "difficulty": "easy",
      "tags": [
        "owasp",
        "xss",
        "web-security"
      ]
    },
    {
      "id": "cyber-004",
      "categoryId": "cybersecurity",
      "question": "What is the Zero Trust security model?",
      "answer": "Zero Trust is a security architecture that eliminates implicit trust based on network location, operating on the principle of 'never trust, always verify.' Every access request is authenticated, authorized, and encrypted regardless of whether it originates inside or outside the corporate network. Key pillars include micro-segmentation (breaking the network into small zones), least-privilege access, continuous verification of device posture and user identity, and assuming breach. Implementation typically involves identity-aware proxies, MFA everywhere, device health checks, and granular access policies. It replaces the traditional castle-and-moat perimeter model that fails once an attacker is inside the network.",
      "difficulty": "easy",
      "tags": [
        "network-security",
        "zero-trust",
        "architecture"
      ]
    },
    {
      "id": "cyber-005",
      "categoryId": "cybersecurity",
      "question": "What is Multi-Factor Authentication (MFA) and why is SMS-based 2FA considered weaker than other methods?",
      "answer": "MFA requires users to present two or more verification factors from different categories: something you know (password), something you have (hardware token, phone), and something you are (biometrics). SMS-based 2FA is considered weaker because it is vulnerable to SIM swapping (attacker convinces carrier to port your number), SS7 protocol interception, and malware that intercepts SMS messages on the device. Stronger alternatives include hardware security keys (FIDO2/WebAuthn), TOTP authenticator apps (Google Authenticator, Authy), and push-based approval (Duo). Hardware security keys are considered the gold standard because they are phishing-resistant and require physical possession.",
      "difficulty": "easy",
      "tags": [
        "authentication",
        "mfa",
        "identity"
      ]
    },
    {
      "id": "cyber-006",
      "categoryId": "cybersecurity",
      "question": "What is a CSRF attack and how do you prevent it?",
      "answer": "Cross-Site Request Forgery (CSRF) tricks an authenticated user's browser into sending unintended requests to a web application where they are already logged in. For example, a malicious page could contain a hidden form that submits a bank transfer request using the user's session cookie. Prevention uses anti-CSRF tokens (a unique, unpredictable value per session or per request that the server validates), the SameSite cookie attribute (Strict or Lax prevents cross-origin cookie sending), checking the Origin and Referer headers, and requiring re-authentication for sensitive actions. Modern frameworks like Django, Rails, and Spring include built-in CSRF token middleware.",
      "difficulty": "easy",
      "tags": [
        "owasp",
        "csrf",
        "web-security"
      ]
    },
    {
      "id": "cyber-007",
      "categoryId": "cybersecurity",
      "question": "Explain the TLS 1.3 handshake process and how it differs from TLS 1.2.",
      "answer": "TLS 1.3 completes the handshake in one round trip (1-RTT) compared to TLS 1.2's two round trips. The client sends a ClientHello with supported cipher suites and key shares (guessing the server's preferred key exchange). The server responds with its key share, certificate, and Finished message in a single flight. This means encrypted data can flow after just one round trip. TLS 1.3 also removed insecure algorithms (RSA key exchange, CBC mode, SHA-1, RC4, DES), eliminated renegotiation, and supports 0-RTT resumption for repeat connections (though 0-RTT is vulnerable to replay attacks). The handshake itself is encrypted after the ServerHello, hiding certificate details from passive observers.",
      "difficulty": "hard",
      "tags": [
        "cryptography",
        "tls",
        "protocols"
      ]
    },
    {
      "id": "cyber-008",
      "categoryId": "cybersecurity",
      "question": "What is the difference between an IDS and an IPS? Where do they sit in a network?",
      "answer": "An Intrusion Detection System (IDS) passively monitors network traffic and alerts on suspicious activity, while an Intrusion Prevention System (IPS) sits inline and can actively block or drop malicious packets in real time. An IDS typically operates via a SPAN/mirror port, receiving a copy of traffic without being in the data path, so it cannot cause latency or availability issues but also cannot stop attacks. An IPS sits directly in the traffic path between the firewall and the internal network, adding slight latency but providing active defense. Both can use signature-based detection (matching known attack patterns) or anomaly-based detection (flagging deviations from baseline behavior). Many modern solutions combine both capabilities as IDPS.",
      "difficulty": "medium",
      "tags": [
        "network-security",
        "ids-ips",
        "monitoring"
      ]
    },
    {
      "id": "cyber-009",
      "categoryId": "cybersecurity",
      "question": "How does OAuth 2.0 work and what are common security pitfalls?",
      "answer": "OAuth 2.0 is a delegation protocol where a resource owner grants a client limited access to their resources on a resource server via an authorization server, without sharing credentials. The Authorization Code flow (recommended for server-side apps) involves redirecting the user to the authorization server, receiving an authorization code, then exchanging it for an access token via a back-channel request. Common security pitfalls include: not validating the redirect_uri (enabling token theft via open redirectors), not using PKCE with public clients (authorization code interception), storing tokens insecurely in localStorage (vulnerable to XSS), overly broad scopes, and not validating the state parameter (CSRF on the callback). Always use the Authorization Code flow with PKCE over the Implicit flow, which is now deprecated.",
      "difficulty": "medium",
      "tags": [
        "authentication",
        "oauth",
        "web-security"
      ]
    },
    {
      "id": "cyber-010",
      "categoryId": "cybersecurity",
      "question": "What is SSRF (Server-Side Request Forgery) and why has it become more critical in cloud environments?",
      "answer": "SSRF is an attack where the attacker causes the server to make HTTP requests to an arbitrary destination, often targeting internal services not directly accessible from the internet. In cloud environments, SSRF is especially dangerous because cloud metadata services (e.g., AWS IMDSv1 at 169.254.169.254) can expose IAM credentials, instance metadata, and secrets. The Capital One breach in 2019 exploited SSRF to extract IAM credentials from the AWS metadata endpoint. Mitigations include using allowlists for permitted destination URLs, blocking requests to private IP ranges and metadata endpoints, using AWS IMDSv2 (requires a session token obtained via PUT request), network-level egress filtering, and running workloads with minimal IAM permissions.",
      "difficulty": "medium",
      "tags": [
        "owasp",
        "ssrf",
        "cloud-security"
      ]
    },
    {
      "id": "cyber-011",
      "categoryId": "cybersecurity",
      "question": "Explain the shared responsibility model in cloud security.",
      "answer": "The shared responsibility model defines which security obligations belong to the cloud provider versus the customer. The provider is responsible for security 'of' the cloud: physical data centers, hypervisor, network infrastructure, and managed service underpinnings. The customer is responsible for security 'in' the cloud: data classification and encryption, IAM policies, OS patching (on VMs), firewall rules, application code security, and compliance. The dividing line shifts based on the service model: in IaaS (EC2) the customer manages the OS and up; in PaaS (Lambda, RDS) the provider handles more of the stack; in SaaS the customer primarily manages access and data. A common mistake is assuming the cloud provider handles everything, leading to misconfigured S3 buckets, overly permissive security groups, and unencrypted data at rest.",
      "difficulty": "medium",
      "tags": [
        "cloud-security",
        "iam",
        "compliance"
      ]
    },
    {
      "id": "cyber-012",
      "categoryId": "cybersecurity",
      "question": "What are JWTs and what security concerns should you consider when using them for authentication?",
      "answer": "JSON Web Tokens are compact, URL-safe tokens with three Base64-encoded parts: header (algorithm, type), payload (claims like sub, exp, iat), and signature. Security concerns include: the 'none' algorithm attack (attacker changes the header alg to 'none' and strips the signature), algorithm confusion (switching from RS256 to HS256 and using the public key as the HMAC secret), not validating expiration (exp) or issuer (iss) claims, storing JWTs in localStorage (XSS-accessible), and the inability to revoke individual tokens before expiry without a server-side blocklist. Best practices are to always validate the algorithm on the server side against an allowlist, use short expiration times with refresh tokens, store tokens in httpOnly secure cookies, and consider using opaque tokens with server-side sessions for high-security applications.",
      "difficulty": "medium",
      "tags": [
        "authentication",
        "jwt",
        "web-security"
      ]
    },
    {
      "id": "cyber-013",
      "categoryId": "cybersecurity",
      "question": "Describe the incident response process. What are the standard phases?",
      "answer": "The NIST incident response lifecycle has four phases. Preparation involves establishing an IR team, creating playbooks, deploying monitoring tools (SIEM, EDR), and running tabletop exercises. Detection and Analysis involves identifying indicators of compromise through alerts, log correlation, and triage to determine scope and severity. Containment, Eradication, and Recovery involves short-term containment (isolating affected systems), long-term containment (applying patches), eradicating the root cause (removing malware, closing vulnerabilities), and restoring systems from clean backups with validation. Post-Incident Activity involves conducting a blameless retrospective, documenting the timeline and lessons learned, updating detection rules and playbooks, and reporting to relevant stakeholders or regulators. The key principle is to preserve forensic evidence during containment rather than immediately wiping systems.",
      "difficulty": "medium",
      "tags": [
        "incident-response",
        "nist",
        "forensics"
      ]
    },
    {
      "id": "cyber-014",
      "categoryId": "cybersecurity",
      "question": "What is the difference between hashing, encryption, and encoding? Give appropriate use cases for each.",
      "answer": "Hashing is a one-way function producing a fixed-size digest (SHA-256, bcrypt); it is irreversible and used for password storage, data integrity verification, and digital signatures. Encryption is a reversible transformation requiring a key, used for protecting data confidentiality (AES for data at rest, TLS for data in transit). Encoding is a reversible format conversion with no secret key (Base64, URL encoding), used purely for data representation and transport compatibility, not for security. A critical mistake is using encoding (Base64) thinking it provides security, or using fast hashes (MD5, SHA-256) for passwords instead of purpose-built slow hashes like bcrypt, scrypt, or Argon2 that resist brute-force attacks through configurable work factors.",
      "difficulty": "medium",
      "tags": [
        "cryptography",
        "hashing",
        "fundamentals"
      ]
    },
    {
      "id": "cyber-015",
      "categoryId": "cybersecurity",
      "question": "What is container security and what are the main attack vectors for containerized applications?",
      "answer": "Container security covers the entire lifecycle: image security, runtime security, and orchestration security. Key attack vectors include vulnerable base images (using unpatched OS images from public registries), secrets baked into images (API keys in Dockerfiles or layers), running containers as root (enabling container escape), exploiting the shared kernel (a container escape vulnerability like CVE-2019-5736 compromises the host), and misconfigured orchestrators (unauthenticated Kubernetes API, overly permissive RBAC). Best practices include scanning images in CI/CD (Trivy, Snyk), using minimal base images (distroless, Alpine), running as non-root with read-only filesystems, applying network policies for pod-to-pod communication, using Kubernetes Pod Security Standards, and signing images with cosign or Notary for supply chain integrity.",
      "difficulty": "medium",
      "tags": [
        "containers",
        "kubernetes",
        "docker"
      ]
    },
    {
      "id": "cyber-016",
      "categoryId": "cybersecurity",
      "question": "What are the key differences between SOC 2, GDPR, and PCI DSS? When does each apply?",
      "answer": "SOC 2 is a voluntary audit framework for service organizations demonstrating controls across five Trust Service Criteria (security, availability, processing integrity, confidentiality, privacy); it applies to SaaS companies and cloud providers whose customers require assurance. GDPR is a European regulation governing the processing of personal data of EU residents; it applies to any organization handling EU personal data regardless of location, with penalties up to 4% of global revenue. PCI DSS is a mandatory standard for any entity that stores, processes, or transmits payment card data, requiring controls like encryption of cardholder data, network segmentation, regular vulnerability scanning, and access controls. SOC 2 is about proving your controls work, GDPR is about data subject rights and privacy by design, and PCI DSS is about protecting cardholder data specifically.",
      "difficulty": "hard",
      "tags": [
        "compliance",
        "gdpr",
        "soc2",
        "pci-dss"
      ]
    },
    {
      "id": "cyber-017",
      "categoryId": "cybersecurity",
      "question": "Explain how a SIEM works and what makes an effective detection rule.",
      "answer": "A Security Information and Event Management system aggregates logs from diverse sources (firewalls, endpoints, applications, cloud services), normalizes them into a common schema, correlates events across sources, and triggers alerts based on detection rules. Effective detection rules combine multiple signals rather than single events: for example, a brute-force detection rule might trigger on 10+ failed logins from one IP within 5 minutes followed by a successful login. Rules should have low false-positive rates (use contextual data like geo-location, time-of-day, baseline behavior), include severity classification, reference MITRE ATT&CK techniques for standardized categorization, and be continuously tuned based on alert feedback. Modern SIEMs (Splunk, Sentinel, Elastic Security) also incorporate UEBA (User and Entity Behavior Analytics) using statistical baselines to detect anomalies that signature-based rules miss.",
      "difficulty": "hard",
      "tags": [
        "siem",
        "monitoring",
        "incident-response"
      ]
    },
    {
      "id": "cyber-018",
      "categoryId": "cybersecurity",
      "question": "What is secrets management and how should secrets be handled in a cloud-native application?",
      "answer": "Secrets management is the practice of securely storing, distributing, rotating, and auditing access to sensitive credentials like API keys, database passwords, certificates, and encryption keys. In cloud-native applications, secrets should never be stored in source code, environment variables (visible in process listings), or container images. Instead, use a dedicated secrets manager (AWS Secrets Manager, HashiCorp Vault, GCP Secret Manager) that provides encryption at rest, fine-grained access control via IAM policies, automatic rotation, and audit logging. Applications should fetch secrets at runtime via the secrets manager's API or use sidecar injection (Vault Agent, External Secrets Operator in Kubernetes). Additional practices include using short-lived credentials over long-lived keys, implementing break-glass procedures for emergency access, and scanning repositories with tools like git-secrets or truffleHog to catch accidental commits.",
      "difficulty": "hard",
      "tags": [
        "cloud-security",
        "secrets-management",
        "devops"
      ]
    },
    {
      "id": "cyber-019",
      "categoryId": "cybersecurity",
      "question": "What are the OWASP Top 10 API Security risks and how do they differ from the traditional web Top 10?",
      "answer": "The OWASP API Security Top 10 (2023) focuses on risks specific to APIs rather than traditional web applications. Key entries include: Broken Object Level Authorization (BOLA, where users access other users' objects by manipulating IDs), Broken Authentication (weak token handling, no rate limiting on auth endpoints), Broken Object Property Level Authorization (mass assignment and excessive data exposure), Unrestricted Resource Consumption (no rate limiting or pagination enabling DoS), Broken Function Level Authorization (user calling admin endpoints), Server-Side Request Forgery, and Security Misconfiguration. The main difference from the web Top 10 is the emphasis on authorization flaws over injection attacks, because APIs expose structured endpoints where logic-level bugs (accessing /api/users/123 when you are user 456) are more prevalent than XSS or CSRF. Mitigation requires implementing proper authorization checks at the object level on every endpoint, not just at the perimeter.",
      "difficulty": "hard",
      "tags": [
        "owasp",
        "api-security",
        "authorization"
      ]
    },
    {
      "id": "cyber-020",
      "categoryId": "cybersecurity",
      "question": "How does certificate pinning work and what are the trade-offs of implementing it?",
      "answer": "Certificate pinning associates a host with its expected X.509 certificate or public key, rejecting connections even if the presented certificate is signed by a trusted CA. This defends against compromised or malicious CAs, rogue certificates, and man-in-the-middle attacks that use a valid but attacker-obtained certificate. Implementation can pin the leaf certificate, intermediate CA, or public key (SPKI hash). The trade-offs are significant: pinning makes certificate rotation operationally difficult (requires app updates before the old cert expires), can brick apps if done incorrectly, breaks corporate TLS inspection proxies, and is ineffective on web browsers (Chrome removed HPKP support due to the risk of hostile pinning). Best practices include pinning the public key rather than the full certificate (survives cert renewal if the key stays the same), including backup pins, and using short pin expiry windows. Mobile apps are the primary use case, while server-to-server communications may prefer mutual TLS instead.",
      "difficulty": "hard",
      "tags": [
        "cryptography",
        "tls",
        "certificate-pinning"
      ]
    }
  ]
}